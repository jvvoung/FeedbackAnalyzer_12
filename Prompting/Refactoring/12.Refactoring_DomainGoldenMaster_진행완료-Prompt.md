# Feedback Analyzer — 대화형 프롬프트 Export

| 항목 | 내용 |
|------|------|
| 대응 보고서 | `Report/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료.md` |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| Export 일자 | 2026-05-22 |
| 세션 범위 | **Refactoring·Domain Golden Master** — GM-D-01~04 baseline·Approval 테스트·Report · Prompt Export |
| 선행 Export | [`11.Green_Phase1_검증완료-Prompt.md`](../Green/11.Green_Phase1_검증완료-Prompt.md) |

---

## Turn 1 — Domain Golden Master (Phase 1)

### User

```
[P] 회귀 테스트(Approval / Golden Master) 설계 전문가입니다.
    HTTP 웹앱 + Domain Service 이중 Golden Master 전략을 설계합니다.
[C] 워크스페이스: c:\DEV\FeedbackAnalyzer_12
    기술 스택: C++17, CMake 3.14+, Google Test, cpp-httplib
    근거: docs/test_plan.md §2·§4, docs/PRD.md §3.2·§6.2, README.md §RED To-Do (TC-A/B)
    ⚠️ Golden 생성 시 프로덕션 로직 변경 금지 — 캡처·기준 파일·approve 테스트만 추가
[T] Feedback Analyzer 출력 기반 Golden Master 기준 파일을 생성해줘.
---
## 1) 기준 출력 생성/보관 전략
### 1-A. Domain Golden (권장 — Phase 1, 안정적)
경로: `tests/golden/domain_golden_expected.txt`  
버전 관리: **필수** (`git add tests/golden/domain_golden_expected.txt`)
입력 시나리오 4개 (test_plan / PRD §2.2 기준):
| 시나리오 ID | Given | When | 캡처 대상 |
|-------------|-------|------|-----------|
| GM-D-01 | Feedback `"배송이 너무 늦어요. 화가 납니다."` | TextAnalyzer::sent + kw | 감정 map(긍/중/부), 키워드 map(배송+1…) |
| GM-D-02 | Feedback `"그냥 그래요. 특별한 감정 없음."` | Filters::fil(중립, 전체) | 필터 결과 text 목록 + 건수 |
| GM-D-03 | CSV `text\n택배가 빨라요.\n` | CsvParser::parse | Feedback text 목록 (1건) |
| GM-D-04 | Feedback 3건 혼합 | Filters::fil(전체, 전체) | 반환 건수=3, text 목록 |
캡처 방식:
- `tests/golden/capture_domain_golden.cpp` 또는 GTest `GoldenMasterCapture` 1회 실행
- stdout 또는 `tests/golden/_actual_domain.txt` 리디렉션
- **타임스탬프·랜덤값 제외** — 순수 Domain 직렬화만 (stable sort)
```

### Assistant (결과 요약)

#### 1) Domain Golden 산출물

| 파일 | 역할 |
|------|------|
| `tests/golden/domain_golden_expected.txt` | GM-D-01~04 Golden baseline (Git 추적) |
| `tests/golden/DomainGoldenCapture.h` | 시나리오 캡처·직렬화·파일 I/O 헬퍼 |
| `tests/GoldenMasterTest.cpp` | Approval + Capture GTest |
| `.gitattributes` | `tests/golden/*.txt text eol=lf` |
| `CMakeLists.txt` | `GoldenMasterTest.cpp`, `GOLDEN_DIR` compile definition |

#### 2) 직렬화 형식

| 요소 | 규칙 |
|------|------|
| 시나리오 구분 | `=== GM-D-XX ===` |
| 감정 map | `sentiment\|{키}\|{건수}` (`std::map` UTF-8 바이트 순) |
| 키워드 map | `keyword\|{키}\|{건수}` |
| 필터/CSV | `count\|{N}` + `text\|{본문}` (입력 순서) |
| 줄바꿈 | LF — `normalizeLineEndings()` + `.gitattributes` |

#### 3) GM-D-01~04 기준 스냅샷

| ID | Then (요약) |
|----|-------------|
| **GM-D-01** | 부정=1, 배송=1, 나머지 0 |
| **GM-D-02** | count=1, `"그냥 그래요. 특별한 감정 없음."` |
| **GM-D-03** | count=1, `"택배가 빨라요."` |
| **GM-D-04** | count=3 (긍정·부정·중립 혼합 3건) |

#### 4) 테스트 구조

**Approval (회귀):**

```cpp
TEST_F(DomainGoldenMasterTest, Given_DomainScenarios_When_Capture_Then_MatchesGoldenFile)
// actual = golden::captureAllDomainGolden()
// expected = readFile(GOLDEN_DIR "/domain_golden_expected.txt")
// EXPECT_EQ(expected, actual)
```

**Capture (의도적 갱신):**

```cpp
TEST(DomainGoldenMasterCapture, GoldenMasterCapture)
// UPDATE_GOLDEN=1 일 때만 baseline 덮어쓰기, 아니면 GTEST_SKIP
```

#### 5) 구현 세부

- `Filters::fil` 내부 `std::cout` — 캡처 시 `suppressStdout()`으로 제외 (Golden에 미포함)
- **프로덕션 코드(`src/cpp/*`) 변경 0건** — 테스트·CMake·gitattributes만
- Windows CRLF 이슈 — Approval 초기 Fail → `normalizeLineEndings()` + `.gitattributes`로 해소

#### 6) ctest 결과

```powershell
cmake --build build_cov --target feedback_analyzer_tests
ctest --test-dir build_cov --output-on-failure
```

| 항목 | 보고서 11 | 본 Turn |
|------|-----------|---------|
| 총 테스트 | 14 | **16** |
| Pass | 14 | **16** |
| Skip | 0 | 1 (`GoldenMasterCapture` — env 미설정) |
| Fail | 0 | **0** |

**실행 명령:**

```powershell
# Approval
ctest --test-dir build_cov -R DomainGoldenMasterTest --output-on-failure

# baseline 갱신 (의도적 1회)
$env:UPDATE_GOLDEN = "1"
ctest --test-dir build_cov -R GoldenMasterCapture --output-on-failure
```

#### 7) HTTP Golden Master

- **미착수** — Phase 2 예정 (`tests/golden/http_golden_expected/`)

---

## Turn 2 — Report 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘. Refactoring폴더를 만들어서 거기에 넣어줘. 파일명은 앞에 숫자는 green폴더안에있는 파일에 이어서 작성하면될거같아
```

### Assistant (결과 요약)

- `Report/Refactoring/` 폴더 생성
- `Report/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료.md` 작성 (Green 11번 이후 **12번**)
- 선행 보고서: `11.Green_Phase1_검증완료.md`
- Executive Summary, GM-D-01~04 매핑, 산출물·ctest 16/16, 보고서 11 대비 diff, 다음 단계(HTTP Golden·HtmlRenderer·AC-4) 정리

---

## Turn 3 — 프롬프트 Export (본 Turn)

### User

```
현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
Refactoring폴더를 만들어줘
```

### Assistant (결과 요약)

- `Prompting/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료-Prompt.md` 생성 (본 문서)

---

## 세션 산출물 매핑

| Turn | User 의도 | 산출물 |
|------|-----------|--------|
| 1 | Domain Golden Master GM-D-01~04 baseline·Approval | `tests/golden/domain_golden_expected.txt`, `DomainGoldenCapture.h`, `GoldenMasterTest.cpp`, `CMakeLists.txt`, `.gitattributes` |
| 2 | Refactoring 세션 진행 보고 | `Report/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료.md` |
| 3 | 대화형 프롬프트 보존 | `Prompting/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료-Prompt.md` |

---

## Green(11)·Refactoring(12) 대비 변경 요약

| 항목 | 11 (Phase 1 검증) | 12 (본 세션 Domain Golden) |
|------|-------------------|----------------------------|
| ctest 건수 | 14 | **16** (+Golden 2) |
| Golden Master | 없음 | **Domain GM-D-01~04 baseline** |
| `tests/golden/` | 없음 | **신규** |
| 프로덕션 diff | 0 | **0** (테스트·CMake·gitattributes만) |
| REFACTOR (HtmlRenderer 등) | 미수행 | **미수행** (회귀 인프라만) |
| AC-4 CsvParser 70% | Partial | **변경 없음** |
| HTTP Golden | — | ⏳ Phase 2 예정 |

---

## 재사용 가이드

### Domain Golden Master 재실행 순서

1. **선행:** `Report/Green/11.Green_Phase1_검증완료.md` (ctest 14/14 Green)
2. **Turn 1** → GM-D-01~04 캡처 헬퍼·baseline·Approval 테스트 추가
3. **Turn 1** → `ctest --test-dir build_cov` → **16/16 Pass** 확인
4. **Turn 2** → `Report/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료.md`
5. **Turn 3** → Prompt Export

### Turn 1 핵심 제약 (재현 시 필수)

```
역할: 회귀 테스트(Approval / Golden Master) — Domain Service Phase 1
원칙: 프로덕션 로직 변경 금지 (src/cpp/* diff 0)

경로:
  tests/golden/domain_golden_expected.txt   # Git 필수
  tests/golden/DomainGoldenCapture.h
  tests/GoldenMasterTest.cpp

시나리오 (test_plan §2·§4):
  GM-D-01  TextAnalyzer::sent+kw  — "배송이 너무 늦어요. 화가 납니다."
  GM-D-02  Filters::fil(중립,전체) — "그냥 그래요. 특별한 감정 없음."
  GM-D-03  CsvParser::parse         — CSV text\n택배가 빨라요.\n
  GM-D-04  Filters::fil(전체,전체)  — 3건 혼합

직렬화:
  sentiment|키|건수 / keyword|키|건수 / count|N / text|본문
  타임스탬프·랜덤값 제외, std::map stable sort
  Filters::fil cout → suppressStdout() (Golden 미포함)

CMake:
  tests/GoldenMasterTest.cpp 추가
  GOLDEN_DIR="${CMAKE_SOURCE_DIR}/tests/golden"

.gitattributes:
  tests/golden/*.txt text eol=lf

검증:
  ctest -R DomainGoldenMasterTest → Pass
  GoldenMasterCapture → UPDATE_GOLDEN≠1 이면 Skip

금지:
  TextAnalyzer.cpp / Filters.h / main.cpp 프로덕션 수정
  httplib.h 수정
  Golden 생성을 위한 분류 규칙 변경
```

### 빌드·검증 명령

```powershell
# 1. 테스트 빌드 + Domain Golden Approval
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build_cov
cmake --build c:\DEV\FeedbackAnalyzer_12\build_cov --target feedback_analyzer_tests
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov -R DomainGoldenMasterTest --output-on-failure
# 기대: 1 passed

# 2. 전체 ctest (기존 14 + Golden 2)
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov --output-on-failure
# 기대: 16 passed, 0 failed, 1 skipped (GoldenMasterCapture)

# 3. baseline 의도적 갱신
$env:UPDATE_GOLDEN = "1"
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov -R GoldenMasterCapture --output-on-failure
Remove-Item Env:UPDATE_GOLDEN
```

### Git 등록 권장

```powershell
git add tests/golden/domain_golden_expected.txt `
        tests/golden/DomainGoldenCapture.h `
        tests/GoldenMasterTest.cpp `
        CMakeLists.txt `
        .gitattributes `
        Report/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료.md `
        Prompting/Refactoring/12.Refactoring_DomainGoldenMaster_진행완료-Prompt.md
```

커밋 메시지 예시:

```
test(refactor): add Domain Golden Master baseline (GM-D-01~04)

Phase 1 Domain Service 회귀 게이트. 프로덕션 로직 변경 없음.
```

---

## 후속 Turn 제안 (미실행)

| Turn | 주제 | User 프롬프트 힌트 |
|------|------|-------------------|
| 4 | HTTP Golden Master | POST `/analyze`·`/filter` HTML stat 캡처, in-process httplib |
| 5 | HtmlRenderer 분리 | Domain Golden Approval Green 유지, `main.cpp` ≤200줄 |
| 6 | CsvParser 경계 UT | 따옴표·CRLF → AC-4 70% → 90%+ |

---

## 변경 이력

| 일자 | Phase | Export |
|------|-------|--------|
| 2026-05-22 | GREEN·검증 | `11.Green_Phase1_검증완료-Prompt.md` |
| 2026-05-22 | REFACTORING | Domain Golden Master GM-D-01~04 — **본 문서** |

---

*본 Export는 Green Phase 1 검증(Export 11) 직후 Domain Golden Master 회귀 인프라 구축 세션(Turn 1~3)의 대화형 프롬프트 스냅샷이다.*
