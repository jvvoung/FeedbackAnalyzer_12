# Feedback Analyzer — 대화형 프롬프트 Export

| 항목 | 내용 |
|------|------|
| 대응 보고서 | `Report/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료.md` |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| Export 일자 | 2026-05-22 |
| 세션 범위 | **Refactoring·Phase 3** — 잔존 스멜 진단 · 네이밍·매직넘버·레거시 정리 · 슬라이드 6대 검증 · Report · Prompt Export |
| 선행 Export | [`17.Refactoring_Exercise4_NamingDesign_진행완료-Prompt.md`](17.Refactoring_Exercise4_NamingDesign_진행완료-Prompt.md) |

---

## Turn 1 — 잔존 코드 스멜 진단 (Read/Grep only)

### User

```
@c:\DEV\FeedbackAnalyzer_12\ 프로젝트에서 현재 리팩토링 진행하면서 남아있는 코드스멜이 어떤것들이 잇는지 파악해줘.
```

### Assistant (결과 요약)

Report 17·Phase 2 이후 코드베이스와 `docs/analysis.md`, `docs/defect_list.md`를 대조하여 **해소됨 vs 잔존** 스멜을 분류.

#### 이미 해소된 스멜

| 스멜 | 상태 |
|------|------|
| God Module (`main.cpp` 372줄) | ✅ 18줄 bootstrap |
| 축약 메서드명 (`sent`/`kw`/`fil`) | ✅ Rename 완료 |
| `containsAny` 중복 | ✅ `KeywordUtils.h` 단일화 |
| 이중 키워드 사전 (M-7) | ✅ `Constants` + `FeedbackClassifier` |
| Fake Session | ✅ `FeedbackSession` |
| `fil_data` 전역 | ✅ `AppState` / `FeedbackSession` |

#### 잔존 스멜 (P1~P3)

| 우선순위 | 스멜 | 위치 |
|----------|------|------|
| P1 | `TextAnalyzer::globalSent/Kw` static | `TextAnalyzer.h` |
| P1 | `Filters::filter()` `std::cout` 부작용 | `Filters.h:36-38` |
| P1 | Logger ↔ UI 불일치 | `Logger.h`, `RouteHandlers` |
| P1 | download 정책·CSV 이스케이프 | `RouteHandlers`, `CsvExporter` |
| P2 | `FileHandler` Dead Code | `FileHandler.h` |
| P2 | Lazy Class (`FeedbackSession`, `UIComponents`) | wrapper만 수행 |
| P2 | `AppState` vs `FeedbackSession` API 이중화 | `RouteHandlers`, `FilterUseCase` |
| P2 | 매직넘버 (`8080`, BOM, `전체`) | `main.cpp`, `CsvExporter` |
| P3 | `HtmlPageRenderer` Long Method | ~120줄 inline HTML |
| P3 | `.gitignore` Spring 잔재 | L-4 |

**ctest:** 20/20 Pass (Report 17 기준).

---

## Turn 2 — Phase 3 네이밍·매직넘버·레거시 정리 구현

### User

```
네이밍& 매직넘버개선을 중점으로 두고 나머지 리팩토링 진행해줘.
```

### Assistant (결과 요약)

네이밍·매직넘버 개선을 중심으로 Report 17 §8 잔여 항목 및 P1 스멜을 구현.

#### 1) 상수 모듈 추출 (매직넘버)

| 파일 | 내용 |
|------|------|
| `ServerConfig.h` | `kBindHost`, `kPort`, `publicUrl()` |
| `CsvConstants.h` | `kUtf8Bom`, `kTextColumnHeader`, `kAttachmentFilename`, `kContentType` |
| `FilterConstants.h` | `kAllSentinel` (`u8"전체"`) |

#### 2) 전역 static·부작용 제거

- `TextAnalyzer::globalSent` / `globalKw` **삭제** → 반환값만 사용, header-only
- `TextAnalyzer.cpp` **삭제**
- `Filters.h` — `std::cout` 루프 제거, `#include <iostream>` 제거
- 변수 Rename: `sentimentCounts`, `keywordCounts`, `sentimentFilter`, `keywordFilter` 등

#### 3) Dead Code·Lazy Class 제거

- `FileHandler.h` **삭제**
- `UIComponents.h` / `UIComponents.cpp` **삭제** → `HtmlPageRenderer`가 `KeywordRegistry` 직접 사용

#### 4) `FeedbackSession` API 통합

```cpp
static const std::vector<Feedback>& getLastFiltered();
static void setLastFiltered(const std::vector<Feedback>& filtered);
static void clear();  // current + lastFiltered
```

- `FilterUseCase.cpp` → `FeedbackSession::setLastFiltered`
- `RouteHandlers.cpp` (download) → `FeedbackSession::getLastFiltered`
- `AppState.h` 직접 include 제거 (RouteHandlers)

#### 5) `CsvExporter` CSV 이스케이프

- `escapeCsvField()` — 쉼표·따옴표·개행 RFC 4180 스타일
- BOM + `text\n` 헤더는 `CsvConstants` 경유, **값 불변**

#### 6) 빌드·ctest 검증

```powershell
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build_cov
cmake --build c:\DEV\FeedbackAnalyzer_12\build_cov
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov --output-on-failure
```

| 항목 | 결과 |
|------|------|
| ctest 전체 | **20 passed, 0 failed, 1 skipped** |
| Golden Master | **4/4 Pass** (baseline 미갱신) |
| P0 Gate (H-1~H-3) | **Pass** |
| `httplib.h` diff | **0** |

**워킹 트리 diff (HEAD 대비):** 13 files, **+76 / −98** lines.

---

## Turn 3 — 교육 슬라이드 6대 레거시 검증

### User

```
이 이미지에 적혀있는 6가지 문제점중에 해결안된게 어떤것들이 있는지 확인해줘
```

*(첨부: 교육 슬라이드 「연습 #4 — Feedback Analyzer 개요 & 레거시 문제점」)*

### Assistant (결과 요약)

슬라이드 6가지 항목을 현재 `src/cpp/` Grep·파일 존재 확인으로 대조.

| # | 슬라이드 문제 | 판정 |
|---|--------------|------|
| 1 | 축약 메서드명 (`sent`, `kw`, `fil`, `fil_data`) | ✅ **해결** |
| 2 | 중복 키워드 등록 (`Constants.cpp`) | ✅ **해결** |
| 3 | `containsAny()` 중복 | ✅ **해결** |
| 4 | 전역 상태 (`fil_data`, `globalSent/Kw`) | ✅ **해결** |
| 5 | 의미 없는 Session (`getOldDataFromSession`) | ✅ **해결** |
| 6 | FileHandler stub | ✅ **해결** |

**판정: 슬라이드 6/6 Resolved.**

> 참고: `AppState`·`Logger::debugMode` 프로세스 전역 static은 아키텍처상 잔존 (슬라이드 명시 대상과 별개).  
> `docs/defect_list.md`는 DEF-005~009 **Open**으로 stale.

---

## Turn 4 — Report 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘.
```

### Assistant (결과 요약)

- `Report/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료.md` 작성
- Report 17 대비 변경, 6파트 구현 상세, 슬라이드 체크리스트, defect_list 권장 갱신, 잔여 항목(CsvParser 커버리지, Logger UI 등) 기록
- 선행 보고서: `17.Refactoring_Exercise4_NamingDesign_진행완료.md`

---

## Turn 5 — 프롬프트 Export (본 Turn)

### User

```
현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
```

### Assistant (결과 요약)

- `Prompting/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료-Prompt.md` 생성 (본 문서)

---

## 세션 산출물 매핑

| Turn | User 의도 | 산출물 |
|------|-----------|--------|
| 1 | 잔존 코드 스멜 진단 | 진단 보고(채팅), P1~P3 분류 |
| 2 | 네이밍·매직넘버·레거시 REFACTOR | `ServerConfig`, `CsvConstants`, `FilterConstants`, 13 files diff |
| 3 | 슬라이드 6대 레거시 검증 | 6/6 Resolved 판정 |
| 4 | 진행 보고 | `Report/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료.md` |
| 5 | 대화형 프롬프트 보존 | `Prompting/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료-Prompt.md` |

---

## Refactoring(17)·Refactoring(18) 대비 변경 요약

| 항목 | 17 (연습 #4) | 18 (본 세션) |
|------|--------------|--------------|
| ctest | 20/0/1 Pass | **20/0/1 Pass** |
| Golden Master | 4/4 Pass | **4/4 Pass (baseline 불변)** |
| `globalSent`/`globalKw` | ⚠️ static 잔존 | ✅ **삭제** |
| `Filters` cout | ⚠️ 잔존 | ✅ **제거** |
| `FileHandler` | ⚠️ 미참조 | ✅ **삭제** |
| `UIComponents` | ⚠️ Lazy wrapper | ✅ **삭제** |
| 매직넘버 | `8080`, BOM, `전체` 인라인 | **`ServerConfig`/`CsvConstants`/`FilterConstants`** |
| `FeedbackSession` | `getCurrent`/`update`/`clear` | **`getLastFiltered`/`setLastFiltered` 추가** |
| CSV escape | ❌ 없음 | ✅ **`CsvExporter::escapeCsvField`** |
| 슬라이드 6대 | #4·#6 잔존 | ✅ **6/6 Resolved** |
| Git 커밋 | ⏳ 미커밋 | ⏳ **미커밋** |

---

## 재사용 가이드

### Phase 3 잔존 스멜 진단 재실행 (Turn 1)

```
[단계] Phase 3 잔존 코드 스멜 진단 — Read/Grep only
프로젝트: c:\DEV\FeedbackAnalyzer_12
근거: docs/analysis.md (L-1, M-2~M-7), docs/defect_list.md, Report/Refactoring/17.*

점검 파일:
  src/cpp/TextAnalyzer.h
  src/cpp/Filters.h
  src/cpp/FileHandler.h          # 존재 시 FAIL
  src/cpp/FeedbackSession.h
  src/cpp/AppState.h
  src/cpp/main.cpp
  src/cpp/CsvExporter.cpp
  src/cpp/RouteHandlers.cpp
  src/cpp/UIComponents.h         # 존재 시 Lazy Class 잔존

체크리스트:
  - globalSent/globalKw Grep 0건
  - Filters.h std::cout 0건
  - FileHandler 참조 0건
  - sent/kw/fil/fil_data Grep 0건
  - containsAny 단일 (KeywordUtils.h)
  - main.cpp ≤ 200줄

금지: 코드·CMake 수정, httplib.h 점검
검증: ctest --test-dir build_cov --output-on-failure
```

### Phase 3 네이밍·상수·레거시 REFACTOR (Turn 2)

```
[단계] Phase 3 REFACTOR — ServerConfig/CsvConstants/FilterConstants + 레거시 정리
프로젝트: c:\DEV\FeedbackAnalyzer_12
선행: Report/Refactoring/17.Refactoring_Exercise4_NamingDesign_진행완료.md
      ctest 20/0/1 Green

작업:
  1. ServerConfig.h — kBindHost, kPort, publicUrl()
  2. CsvConstants.h — kUtf8Bom, kTextColumnHeader, kAttachmentFilename, kContentType
  3. FilterConstants.h — kAllSentinel (u8"전체")
  4. main.cpp — ServerConfig 사용
  5. TextAnalyzer — globalSent/Kw 삭제, TextAnalyzer.cpp 삭제 (header-only)
  6. Filters.h — std::cout 제거, FilterConstants, 파라미터 Rename
  7. FileHandler.h 삭제
  8. UIComponents.h/cpp 삭제 → HtmlPageRenderer → KeywordRegistry 직접
  9. FeedbackSession — getLastFiltered/setLastFiltered; clear() both vectors
 10. FilterUseCase/RouteHandlers — FeedbackSession API 통합
 11. CsvExporter — CsvConstants + escapeCsvField()
 12. CMakeLists.txt — TextAnalyzer.cpp, UIComponents.cpp 제거

금지:
  Golden Master baseline 무분별 갱신 (GOLDEN_UPDATE=1)
  httplib.h / PRD §7.2 HTTP 계약 변경 (5 path, BOM+text\n, port 8080)
  테스트 삭제·완화·skip 추가

검증:
  cmake --build build_cov
  ctest --test-dir build_cov --output-on-failure
  # 기대: 20 passed, 0 failed, 1 skipped
  ctest --test-dir build_cov -R GoldenMaster --output-on-failure
  # 기대: GM-D-01~04 Pass
```

### 슬라이드 6대 레거시 검증 (Turn 3)

```
[단계] 연습 #4 슬라이드 6대 레거시 최종 검증 — Read/Grep only
프로젝트: c:\DEV\FeedbackAnalyzer_12

슬라이드 항목:
  1. sent/kw/fil/fil_data 축약·전역
  2. Constants.cpp 중복 키워드
  3. containsAny 중복
  4. fil_data, globalSent/Kw
  5. Session getOldDataFromSession
  6. FileHandler stub

Grep 패턴:
  sent\(|kw\(|fil\(|fil_data|globalSent|globalKw|getOldDataFromSession|FileHandler

기대: 전 패턴 0건 (FileHandler.h 파일 없음)
출력: 6항목 PASS/FAIL 표 + 종합 판정
```

### 빌드·검증 명령

```powershell
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build_cov
cmake --build c:\DEV\FeedbackAnalyzer_12\build_cov
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov --output-on-failure
# 기대: 20 passed, 0 failed, 1 skipped

ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov -R GoldenMaster --output-on-failure
# 기대: 4/4 Pass

ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov `
  -R "FiltersTest|CsvParserTest|TextAnalyzerTest|KeywordUtilsTest" --output-on-failure
# 기대: P0 Gate Pass
```

### 잔여 항목 후속 프롬프트 예시

```
[단계] Phase 4 — defect_list 갱신 + Logger UI + CsvParser 커버리지
프로젝트: c:\DEV\FeedbackAnalyzer_12
선행: Report/Refactoring/18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료.md

작업:
  - docs/defect_list.md DEF-005~009 상태 갱신 (Fixed/Partial)
  - Logger warning/error → PageViewModel UI 연동 (M-4, AC-6)
  - CsvParser UT 보강 → 커버리지 ≥90% (AC-4)
  - .gitignore Spring Boot 잔재 제거 (L-4)

검증: ctest 20/0/1, GM 4/4, httplib.h diff 0
```

### 권장 커밋 메시지 (참고)

```text
refactor(phase3): extract config constants and remove legacy smells

Add ServerConfig, CsvConstants, FilterConstants.
Remove TextAnalyzer globalSent/Kw, Filters cout, FileHandler, UIComponents.
Unify state access via FeedbackSession; add CSV field escaping.
```

### 변경·신규 파일 목록 (본 세션)

```
src/cpp/
├── ServerConfig.h              # 신규 — 포트·바인드·publicUrl
├── CsvConstants.h              # 신규 — BOM·헤더·MIME·파일명
├── FilterConstants.h             # 신규 — kAllSentinel
├── TextAnalyzer.h              # globalSent/Kw 삭제·네이밍
├── TextAnalyzer.cpp            # 삭제
├── Filters.h                   # cout 제거·상수·네이밍
├── FeedbackSession.h           # getLastFiltered/setLastFiltered
├── FilterUseCase.cpp           # FeedbackSession 경유
├── RouteHandlers.cpp           # FeedbackSession 통합
├── CsvExporter.cpp             # CsvConstants·escape
├── HtmlPageRenderer.cpp        # KeywordRegistry 직접
├── main.cpp                    # ServerConfig
├── FileHandler.h               # 삭제
├── UIComponents.h              # 삭제
└── UIComponents.cpp            # 삭제

CMakeLists.txt                  # TextAnalyzer.cpp, UIComponents.cpp 제거

Report/Refactoring/
└── 18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료.md

Prompting/Refactoring/
└── 18.Refactoring_Phase3_NamingConstants_LegacyCleanup_진행완료-Prompt.md   ← 본 문서
```

---

## 변경 이력

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-22 | Turn 1~5 대화형 프롬프트 Export (Phase 3 네이밍·상수·레거시 정리 세션) |
