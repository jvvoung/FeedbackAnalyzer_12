# Feedback Analyzer — 대화형 프롬프트 Export

| 항목 | 내용 |
|------|------|
| 대응 보고서 | `Report/Green/09.Green_TC-B-02_진행완료.md` |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| Export 일자 | 2026-05-22 |
| 세션 범위 | Phase 1 GREEN — TC-B-02 (T-03 / AC-1 / H-1) 최소 프로덕션 수정 · Report · Prompt Export |
| 선행 Export | [`08.Red_GTestSkeleton_진행완료-Prompt.md`](../Red/08.Red_GTestSkeleton_진행완료-Prompt.md) |

---

## Turn 1 — TDD GREEN (TC-B-02)

### User

```
[단계] TDD GREEN만 수행한다. REFACTOR는 하지 않는다.
프로젝트: c:\DEV\FeedbackAnalyzer_12

대상 테스트: TC-B-02
  파일: tests/FiltersTest.cpp
  테스트: Given_NeutralText_When_FilterSentimentNeutral_Then_MatchesTextAnalyzerSet
  내용(구현 후):
    // Given: Feedback("그냥 그래요. 특별한 감정 없음.")
    // When:  Filters::fil(feedbacks, u8"중립", u8"전체")
    // Then:  결과 집합 == TextAnalyzer가 동일 입력에 대해 중립으로 분류한 집합
    EXPECT_EQ(filtered.size(), 1u);
    // TextAnalyzer 중립 subset과 100% 일치 (AC-1)

  test_plan: T-03, AC-1, H-1
  defect: DEF-001 (docs/defect_list.md)

PRD §3.2 F-06 / §3.3 비즈니스 로직:
  - 감정 판정 순서: 긍정 → 부정 → 중립 (기본값)
  - TextAnalyzer·Filters **동일 규칙** 사용
  - 감정 키워드 **단일 소스**: Constants::SENTIMENT_KEYWORDS
    (Filters::S_KEYWORDS / initFilterKeywords() 의존 제거 — 이번 TC만 최소 범위)
  - 키워드 문자열 u8"긍정"|u8"중립"|u8"부정" 인라인 하드코딩 금지 → Constants 참조

할 일:
1. cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure
   → TC-B-02 (및 RED 스켈레톤) FAIL 확인
2. **최소 프로덕션 수정** — Filters.cpp(또는 Filters.h)만:
   · sentiment=중립 판정을 TextAnalyzer와 동일하게 (긍/부 미매칭 → 중립)
   · S_KEYWORDS[u8"중립"] 전용 키워드 조건 제거 또는 Constants 단일 소스로 교체
3. tests/FiltersTest.cpp — 해당 TC만 FAIL() 제거, Given-When-Then assertion 작성
4. cmake --build build && ctest --test-dir build -R "Given_NeutralText_When_FilterSentimentNeutral" → PASS 확인

금지:
- 다른 TC (TC-B-01, TC-B-03~08, TC-A-01~08) 동시 구현·수정
- REFACTOR (HtmlRenderer 분리, AppState, Rename sent/kw/fil, main.cpp 대규모 변경) 이번 작업에서 금지
- src/cpp/httplib.h, Feedback.h 시그니처 변경 금지
- HTTP 5엔드포인트·감정 3분류 라벨 변경 금지
- H-2/H-3(T-05/T-06) 이번 커밋에서 수정 금지

산출물:
- TC-B-02가 통과하는 최소 프로덕션 코드 (Filters ± Constants 최소 diff)
- TC-B-02 테스트 본문 (FAIL 제거)
- 커밋 메시지 제안: feat(green): fix neutral filter to match TextAnalyzer (AC-1, H-1)
```

### Assistant (결과 요약)

- **RED 확인:** ctest Test #8 Fail (`FAIL() << "RED"`) — 스켈레톤 상태 확인
- **프로덕션 수정 (최소 범위):**
  - `src/cpp/Filters.h` — `S_KEYWORDS` 제거; `Constants::SENTIMENT_KEYWORDS` 참조; 판정 순서 긍정 → 부정 → 중립(기본값); `S_KEYWORDS[u8"중립"]` 전용 조건 제거
  - `src/cpp/Filters.cpp` — `S_KEYWORDS` 정의·`initFilterKeywords()` 본문 삭제 → 빈 stub (`main.cpp:235` 호출 호환)
- **테스트 수정 (TC-B-02만):**
  - `tests/FiltersTest.cpp` — `SetUp()`(`Constants::init()`), Given-When-Then assertion, `getAnalyzerNeutralSubset` 100% 일치 검증
  - 나머지 3건 FiltersTest RED 스켈레톤 유지
- **GREEN 검증:** `ctest -R "Given_NeutralText_When_FilterSentimentNeutral"` → **Pass**
- **ctest 전체:** 1/14 Pass (TC-B-02만 Green), 13건 RED 유지
- **REFACTOR:** 미수행 (금지 준수)
- **커밋 메시지 제안:** `feat(green): fix neutral filter to match TextAnalyzer (AC-1, H-1)`

**핵심 diff (Filters.h 감성 블록):**

```cpp
// As-Is
if (containsAny(txt, S_KEYWORDS[u8"긍정"])) { ... }
else if (containsAny(txt, S_KEYWORDS[u8"부정"])) { ... }
else if (containsAny(txt, S_KEYWORDS[u8"중립"])) { ... }

// To-Be (TextAnalyzer.h:29-34와 동일)
if (containsAny(txt, Constants::SENTIMENT_KEYWORDS[u8"긍정"])) { ... }
else if (containsAny(txt, Constants::SENTIMENT_KEYWORDS[u8"부정"])) { ... }
// 긍/부 미매칭 → currentSentiment = u8"중립" (초기값)
```

**TC-B-02 assertion:**

```cpp
EXPECT_EQ(filtered.size(), 1u);
EXPECT_TRUE(test_support::feedbackSetsEqualByText(filtered, expectedNeutral));
```

---

## Turn 2 — Report 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘.
Green폴더를 새로만들면 될거같아. 파일명은 다른거와 같은 포맷으로 진행해줘
```

### Assistant (결과 요약)

- `Report/Green/01.Green_TC-B-02_진행완료.md` 작성 (초版)
- 보고서 08(Red 스켈레톤) 대비 diff, DEF-001/H-1 기술 상세, ctest 1/14, AC-1 달성, 다음 GREEN 단계 정리
- **사용자 후속 조치:** 보고서 파일명을 `09.Green_TC-B-02_진행완료.md`로 변경

---

## Turn 3 — 프롬프트 Export (본 Turn)

### User

```
파일명을 09.Green_TC-B-02_진행완료.md 이렇게 변경했어. 현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
여기도Green폴더를 생성해줘
```

### Assistant (결과 요약)

- `Prompting/Green/09.Green_TC-B-02_진행완료-Prompt.md` 생성 (본 문서)

---

## 세션 산출물 매핑

| Turn | User 의도 | 산출물 |
|------|-----------|--------|
| 1 | TC-B-02 GREEN — H-1 최소 수정 + assertion | `src/cpp/Filters.h`, `src/cpp/Filters.cpp`, `tests/FiltersTest.cpp` (TC-B-02만) |
| 2 | GREEN Phase 진행 보고 | `Report/Green/09.Green_TC-B-02_진행완료.md` |
| 3 | 대화형 프롬프트 보존 | `Prompting/Green/09.Green_TC-B-02_진행완료-Prompt.md` |

---

## Red(08)·Green(09) 대비 본 세션 변경 요약

| 항목 | 08 (Red 스켈레톤) | 09 (본 세션 Green) |
|------|-------------------|---------------------|
| `src/cpp/Filters.*` | 미수정 | ✅ **수정** (H-1 최소 범위) |
| TC-B-02 본문 | `FAIL() << "RED"` | ✅ Given-When-Then assertion |
| ctest Test #8 | ❌ Fail | ✅ **Pass** |
| ctest 전체 | 0/14 Pass | **1/14 Pass** |
| `S_KEYWORDS` / `initFilterKeywords()` | Filters 전용 사전 사용 | ✅ **미사용** (stub) |
| REFACTOR | 금지 | ❌ 미수행 |
| 다른 TC | Red | **Red 유지** (TC-B-01, 03~08) |
| Report | `Report/Red/08.*` | **`Report/Green/09.Green_TC-B-02_진행완료.md`** |

---

## 재사용 가이드

### GREEN TC-B-02 단계 재실행 순서

1. Turn 1 → RED 확인 → Filters 최소 수정 → TC-B-02 assertion → ctest Pass
2. Turn 2 → `Report/Green/09.Green_TC-B-02_진행완료.md`
3. Turn 3 → Prompt Export

### Turn 1 핵심 제약 (재현 시 필수)

```
단계: TDD GREEN만 — REFACTOR 금지
범위: TC-B-02 단일 테스트만 (FiltersTest.Given_NeutralText_...)
프로덕션: Filters.h / Filters.cpp 최소 diff
  - Constants::SENTIMENT_KEYWORDS 단일 소스
  - 판정: 긍정 → 부정 → 중립(기본값)
  - S_KEYWORDS[u8"중립"] 전용 조건 제거
  - initFilterKeywords() → 빈 stub (main.cpp 호출 유지)
금지:
  - 다른 TC 동시 구현·수정
  - HtmlRenderer, AppState, rename, main.cpp 대규모 변경
  - Feedback.h, httplib.h 시그니처 변경
  - H-2/H-3 (TC-B-04/05) 수정
테스트:
  - SetUp: Constants::init()
  - getAnalyzerNeutralSubset() 100% 일치
  - EXPECT_EQ(filtered.size(), 1u)
검증:
  ctest -R "Given_NeutralText_When_FilterSentimentNeutral" → Pass
```

### 다음 단계 (GREEN 잔여) 프롬프트 예시

```
[단계] TDD GREEN만 수행. REFACTOR 금지.
프로젝트: c:\DEV\FeedbackAnalyzer_12
선행: Report/Green/09.Green_TC-B-02_진행완료.md (ctest 1/14)

[T] 다음 TC 1건만 Green:
  - TC-B-04 (T-05, AC-2, H-2, DEF-002) — CsvParser text 컬럼
  또는
  - TC-B-05 (T-06, AC-3, H-3, DEF-003) — Filters main 키워드 skip

금지: TC-B-02 재수정, REFACTOR, 다른 TC 동시 구현
[F] 해당 TC assertion + 최소 프로덕션 diff, ctest -R 단일 Pass
```

### P0 GREEN 진행 현황

| TC | test_plan | 결함 | 상태 | 수정 대상 |
|----|-----------|------|------|-----------|
| **TC-B-02** | T-03, AC-1 | DEF-001 / H-1 | ✅ **Green** | `Filters.h`, `Filters.cpp` |
| TC-B-04 | T-05, AC-2 | DEF-002 / H-2 | ⏳ Red | `tests/support/CsvParser.cpp`, `main.cpp` |
| TC-B-05 | T-06, AC-3 | DEF-003 / H-3 | ⏳ Red | `Filters.h` (카테고리 `main` skip) |

### 빌드·검증 명령

```powershell
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build
cmake --build c:\DEV\FeedbackAnalyzer_12\build
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build --output-on-failure -R "Given_NeutralText_When_FilterSentimentNeutral"
# 기대: 1 passed (TC-B-02)

ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build --output-on-failure
# 현재: 1 passed, 13 failed (14 total)
```

### 변경 파일 목록 (본 세션)

```
src/cpp/
├── Filters.h          # S_KEYWORDS 제거, Constants::SENTIMENT_KEYWORDS 참조
└── Filters.cpp        # initFilterKeywords() stub

tests/
└── FiltersTest.cpp    # SetUp + TC-B-02 assertion (TC-B-01/03/05 RED 유지)

Report/Green/
└── 09.Green_TC-B-02_진행완료.md

Prompting/Green/
└── 09.Green_TC-B-02_진행완료-Prompt.md   ← 본 문서
```

### 권장 커밋 메시지

```
feat(green): fix neutral filter to match TextAnalyzer (AC-1, H-1)
```

---

## 변경 이력

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-22 | Turn 1~3 대화형 프롬프트 Export (GREEN TC-B-02 세션) |
