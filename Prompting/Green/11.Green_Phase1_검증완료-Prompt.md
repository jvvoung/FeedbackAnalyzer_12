# Feedback Analyzer — 대화형 프롬프트 Export

| 항목 | 내용 |
|------|------|
| 대응 보고서 | `Report/Green/11.Green_Phase1_검증완료.md` |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| Export 일자 | 2026-05-22 |
| 세션 범위 | Phase 1 GREEN **검증** — ctest 전체 실행, gcov/gcovr 커버리지, AC·품질 게이트, 문서 갱신, Report · Prompt Export |
| 선행 Export | [`10.Green_Phase1_전체완료-Prompt.md`](10.Green_Phase1_전체완료-Prompt.md) |

---

## Turn 1 — Phase 1 GREEN 검증 (ctest · 커버리지 · 문서)

### User

```
모든 TC(TC-A-01~08, TC-B-01~08)가 통과한 후 전체 테스트를 실행하라.

프로젝트: c:\DEV\FeedbackAnalyzer_12
근거: README.md §RED 단계 To-Do, docs/test_plan.md §6~§7, docs/PRD.md §7.1 (AC-4~AC-5)

---

## 1) 전체 테스트 실행

cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe
cmake --build build
ctest --test-dir build -V --output-on-failure

(커버리지 빌드 시)
cmake -S . -B build -DENABLE_COVERAGE=ON
cmake --build build
ctest --test-dir build --output-on-failure

---

## 2) 커버리지 측정 (gcov / lcov)

# 측정 대상 (Service / Domain — docs/test_plan.md §7.2)
#   src/cpp/TextAnalyzer.cpp
#   src/cpp/Filters.cpp
#   src/cpp/CsvParser.cpp  (또는 main.cpp에서 추출된 파서)
#   Feedback 관련

lcov --capture --directory build --output-file coverage.info
lcov --remove coverage.info '*/tests/*' '*/googletest/*' '*/httplib.h' --output-file coverage.filtered.info
genhtml coverage.filtered.info --output-directory build/coverage_html

# 제외: main.cpp(HTTP), httplib.h (부록 A 수동 검증)

---

## 3) 확인 항목

### 테스트
- [ ] ctest **0 failures** — TC-A-01~08, TC-B-01~08 전부 PASS
- [ ] TC-B-02 (T-03 / AC-1 / H-1) — 중립 필터 = TextAnalyzer 중립 집합 100% 일치
- [ ] TC-B-04 (T-05 / AC-2 / H-2) — CSV `text` 컬럼 파싱, fields[0] 미사용
- [ ] TC-B-05 (T-06 / AC-3 / H-3) — keyword=배송 시 main 키워드 포함
- [ ] TC-A-08 — docs/PRD.md 부록 A HTTP-S-01~05 회귀 (AC-5)

### 커버리지 (docs/test_plan.md §6.1~§6.2)
- [ ] TextAnalyzer / Filters / CsvParser / Feedback: 각 **≥ 90%** (PRD AC-4 필수)
- [ ] Domain (Service) stretch: **≥ 95%** (TextAnalyzer + Filters + CsvParser 합산)
- [ ] Boundary (T-01~T-11, EX-01~EX-10, Track A): **≥ 85%**
- [ ] HTTP 핸들러(main.cpp): 90% 목표 **아님** — 부록 A 체크리스트로 대체

### 코드 품질 · 아키텍처 (REFACTOR는 별 PR — 이번은 검증만)
- [ ] 감정 키워드가 `Constants::SENTIMENT_KEYWORDS` **단일 소스** (Filters::S_KEYWORDS / initFilterKeywords() 미사용)
- [ ] 감정·카테고리 라벨 `u8"긍정"|u8"중립"|u8"부정"` **인라인 하드코딩 잔존 없음** (Constants 참조)
- [ ] `containsAny` TextAnalyzer·Filters **중복 없음** (KeywordUtils 통합 또는 Phase 3 예정 명시)
- [ ] H-1 / H-2 / H-3 — docs/defect_list.md 상태 **Fixed**
- [ ] `httplib.h` diff **0**
- [ ] HTTP 5엔드포인트 path/method·CSV BOM/`text\n` 형식 **변경 없음** (PRD §7.2)

### (Phase 2 이후 추가 확인 — Phase 1 Green만이면 Skip)
- [ ] main.cpp ≤ 200줄 (HtmlRenderer·RouteHandlers 분리 후)
- [ ] God Function 분해 완료 여부

---

## 4) 산출물

- ctest 전체 로그 (0 failures)
- `build/coverage_html/index.html` 또는 lcov 리포트
- `coverage_baseline_phase1.info` (docs/test_plan.md §7.4)
- docs/defect_list.md H-1~H-3 → Fixed 업데이트
- README §RED To-Do — 통과 TC 체크박스 [x] 반영

---

## 5) 이후 워크플로

green (Phase 1 전 TC Green)
  → feature/A_01 PR 생성 (예: `feature/phase1-green-p0-fixes`)
  → PR 본문: AC-1~AC-5 체크리스트, ctest 결과, 커버리지 스크린샷
  → 리뷰 (회귀: TC-B-02 중립 필터, CSV text, main 키워드)
  → 머지
  → Phase 2 REFACTOR 브랜치 분리 (HtmlRenderer, RouteHandlers — GREEN 유지)

UnitConverter 전용(3.28084, 1.09361, convert, Catch2, TC-A/B 07까지만) 항목 금지.
```

### Assistant (결과 요약)

#### 1) ctest 전체 실행

```powershell
cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe
cmake --build build
ctest --test-dir build -V --output-on-failure
```

| 항목 | 결과 |
|------|------|
| 총 테스트 | **14/14 Pass (0 failures)** |
| 소요 시간 | ~0.40s |
| 상세 로그 | `build/ctest_full_verbose.log` |

**핵심 Gate 테스트:**

| TC | Test # | 결과 |
|----|--------|------|
| TC-B-02 (H-1) | #8 `Given_NeutralText_When_FilterSentimentNeutral_...` | ✅ Pass |
| TC-B-04 (H-2) | #4~6 `CsvParserTest.*` | ✅ Pass |
| TC-B-05 (H-3) | #10 `Given_MainKeywordOnly_When_FilterDelivery_...` | ✅ Pass |

#### 2) 커버리지 측정

- `CMakeLists.txt`에 `ENABLE_COVERAGE` 옵션 **미구현** → `-DCMAKE_CXX_FLAGS="--coverage -O0 -g"` 로 `build_cov/` 별도 빌드
- `lcov` **미설치** → **gcovr 8.6** `--lcov` 로 baseline 생성

```powershell
cmake -S . -B build_cov -G "MinGW Makefiles" `
  -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe `
  -DCMAKE_CXX_FLAGS="--coverage -O0 -g" `
  -DCMAKE_EXE_LINKER_FLAGS="--coverage"
cmake --build build_cov
ctest --test-dir build_cov --output-on-failure
gcovr --root . --object-directory build_cov --filter "src/cpp/" `
  --exclude "tests/" --exclude "googletest" --exclude "httplib.h" `
  --html --html-details -o build_cov/coverage_html/index.html --print-summary
gcovr --root . --object-directory build_cov --filter "src/cpp/" `
  --exclude "tests/" --exclude "googletest" --exclude "httplib.h" `
  --lcov -o coverage_baseline_phase1.info
```

**모듈별 Line Coverage:**

| 대상 | 파일 | Cover | ≥90% |
|------|------|-------|------|
| TextAnalyzer | `src/cpp/TextAnalyzer.h` | **94%** | ✅ |
| Filters | `src/cpp/Filters.h` | **96%** | ✅ |
| Feedback | `src/cpp/Feedback.h` | **100%** | ✅ |
| CsvParser | `tests/support/CsvParser.cpp` | **70%** | ❌ |
| Domain 합산 | TA+Filters+CsvParser+Feedback | **82.7%** | ❌ (stretch 95% 미달) |

**CsvParser 미커버:** 따옴표 이스케이프(`""`), `unquoteField`, CRLF, 짧은 행 skip (lines 15-17, 43-55, 69-74, 112)

#### 3) 확인 항목 결과

| 구분 | 항목 | 결과 |
|------|------|------|
| 테스트 | ctest 0 failures | ✅ 14/14 |
| 테스트 | TC-B-02 / B-04 / B-05 / A-08 | ✅ |
| 커버리지 | 각 모듈 ≥90% | ⚠️ CsvParser **70%** |
| 커버리지 | Domain stretch ≥95% | ❌ **82.7%** |
| 품질 | `S_KEYWORDS` / `initFilterKeywords()` | ✅ src **0건** |
| 품질 | 감정 라벨 Constants 참조 | ⚠️ `u8"긍정"|u8"중립"|u8"부정"` 인라인 잔존 |
| 품질 | `containsAny` 중복 | ⚠️ Phase 3 예정 (TextAnalyzer·Filters 각 static) |
| 품질 | H-1~H-3 defect_list | ✅ **Fixed** 갱신 |
| 품질 | `httplib.h` diff | ✅ **0** |
| 품질 | HTTP 5엔드포인트 계약 | ✅ 변경 없음 |
| Phase 2 | main.cpp ≤200줄 | ⏭️ Skip (366줄) |

#### 4) 산출물·문서 갱신

| 산출물 | 경로 |
|--------|------|
| ctest verbose 로그 | `build/ctest_full_verbose.log` |
| lcov baseline | `coverage_baseline_phase1.info` |
| HTML 커버리지 | `build/coverage_html/index.html` |
| defect_list H-1~H-3 Fixed | `docs/defect_list.md` |
| README §RED To-Do `[x]` | `README.md` (커버리지 미달 명시) |

**AC-4 판정:** ctest Green ✅ / Service 커버리지 ≥90% ⚠️ **Partial** (CsvParser 미달)

---

## Turn 2 — Report 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘.
```

### Assistant (결과 요약)

- `Report/Green/11.Green_Phase1_검증완료.md` 작성
- ctest 14/14 재확인, gcovr 커버리지, AC-1~AC-7 매핑, defect_list·README 갱신, 보고서 10 대비 diff, 다음 단계(PR·CsvParser UT·Phase 2)
- 선행 보고서: `10.Green_Phase1_전체완료.md`

---

## Turn 3 — 프롬프트 Export (본 Turn)

### User

```
현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
```

### Assistant (결과 요약)

- `Prompting/Green/11.Green_Phase1_검증완료-Prompt.md` 생성 (본 문서)

---

## 세션 산출물 매핑

| Turn | User 의도 | 산출물 |
|------|-----------|--------|
| 1 | Phase 1 GREEN 검증 — ctest·커버리지·문서 | `build/ctest_full_verbose.log`, `coverage_baseline_phase1.info`, `build/coverage_html/`, `docs/defect_list.md`, `README.md` |
| 2 | 검증 세션 진행 보고 | `Report/Green/11.Green_Phase1_검증완료.md` |
| 3 | 대화형 프롬프트 보존 | `Prompting/Green/11.Green_Phase1_검증완료-Prompt.md` |

---

## Green(10)·Green(11) 대비 변경 요약

| 항목 | 10 (Phase 1 GREEN) | 11 (본 세션 검증) |
|------|---------------------|-------------------|
| ctest | 14/14 Pass | **14/14 Pass (재확인)** |
| defect_list H-1~H-3 | Open (RED 스냅샷 잔존) | **Fixed** |
| README To-Do | 미체크 `[ ]` | **전 TC `[x]`** |
| lcov baseline | 미측정 | **`coverage_baseline_phase1.info`** |
| AC-4 | Partial (미측정) | **Partial (CsvParser 70%)** |
| HTML 커버리지 | 없음 | **`build/coverage_html/`** |
| 프로덕션 코드 | 16 Green 커밋 | **변경 없음 (검증·문서만)** |
| REFACTOR | 금지 | ❌ 미수행 |

---

## 재사용 가이드

### Phase 1 GREEN 검증 재실행 순서

1. **선행:** `Report/Green/10.Green_Phase1_전체완료.md` (ctest 14/14 Green)
2. **Turn 1** → ctest 전체 `-V` 실행 → 0 failures 확인
3. **Turn 1** → `build_cov/` 커버리지 빌드 + gcovr baseline
4. **Turn 1** → `docs/defect_list.md` H-1~H-3 Fixed, `README.md` To-Do `[x]`
5. **Turn 2** → `Report/Green/11.Green_Phase1_검증완료.md`
6. **Turn 3** → Prompt Export

### Turn 1 핵심 제약 (재현 시 필수)

```
단계: 검증만 — REFACTOR·신규 TC 추가 금지 (AC-4 미달 시 CsvParser UT는 별도 Turn)
원칙: 프로덕션 diff 0 (문서·산출물만 갱신)

빌드·테스트:
  cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe
  cmake --build build
  ctest --test-dir build -V --output-on-failure
  # 기대: 14 passed, 0 failed

커버리지 (ENABLE_COVERAGE 미구현 시):
  cmake -S . -B build_cov ... -DCMAKE_CXX_FLAGS="--coverage -O0 -g" ...
  gcovr --lcov -o coverage_baseline_phase1.info
  gcovr --html -o build/coverage_html/index.html

확인 Gate:
  TC-B-02 Test #8 — 중립 필터 = TextAnalyzer 중립 집합
  TC-B-04 Test #4~6 — CSV text 컬럼, fields[0] 미사용
  TC-B-05 Test #10 — keyword=배송, main 키워드
  httplib.h diff 0
  S_KEYWORDS / initFilterKeywords() src 0건

산출물:
  build/ctest_full_verbose.log
  coverage_baseline_phase1.info
  build/coverage_html/index.html
  docs/defect_list.md (H-1~H-3 Fixed)
  README.md §RED To-Do [x]

금지:
  UnitConverter (3.28084, Catch2 등) 항목
  REFACTOR (HtmlRenderer, main 분해)
  httplib.h 수정
```

### 빌드·검증 명령

```powershell
# 1. 일반 빌드 + ctest
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build `
  -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe
cmake --build c:\DEV\FeedbackAnalyzer_12\build
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build -V --output-on-failure
# 기대: 14 passed, 0 failed

# 2. 커버리지 빌드 + gcovr
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build_cov `
  -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=C:/mingw64/bin/g++.exe `
  -DCMAKE_CXX_FLAGS="--coverage -O0 -g" -DCMAKE_EXE_LINKER_FLAGS="--coverage"
cmake --build c:\DEV\FeedbackAnalyzer_12\build_cov
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov --output-on-failure

gcovr --root c:\DEV\FeedbackAnalyzer_12 --object-directory c:\DEV\FeedbackAnalyzer_12\build_cov `
  --filter "src/cpp/" --filter "tests/support/CsvParser" `
  --exclude "googletest" --print-summary

gcovr --root c:\DEV\FeedbackAnalyzer_12 --object-directory c:\DEV\FeedbackAnalyzer_12\build_cov `
  --filter "src/cpp/" --exclude "tests/" --exclude "googletest" --exclude "httplib.h" `
  --lcov -o c:\DEV\FeedbackAnalyzer_12\coverage_baseline_phase1.info

# 3. Gate 테스트 단독
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build -R "Given_NeutralText_When_FilterSentimentNeutral" --output-on-failure
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build -R "CsvParserTest" --output-on-failure
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build -R "Given_MainKeywordOnly_When_FilterDelivery" --output-on-failure
```

### AC-4 미달 해소 프롬프트 예시 (후속 Turn)

```
[단계] TDD GREEN — CsvParser 커버리지 ≥90% (AC-4 완료)
프로젝트: c:\DEV\FeedbackAnalyzer_12
선행: Report/Green/11.Green_Phase1_검증완료.md (CsvParser 70%)

대상: tests/CsvParserTest.cpp
  - Given: CSV 따옴표 필드 ("hello","world")
  - Given: CRLF 줄바꿈 (\r\n)
  - Given: textIndex 초과 짧은 행 skip

금지: REFACTOR, httplib.h, ctest 14/14 회귀
검증: gcovr CsvParser ≥90%, ctest 14/14 Pass
```

### PR 생성 프롬프트 예시 (후속 Turn)

```
[단계] feature/phase1-green-p0-fixes PR 생성
프로젝트: c:\DEV\FeedbackAnalyzer_12
선행: Report/Green/11.Green_Phase1_검증완료.md

PR 본문:
  - AC-1~AC-5 체크리스트
  - ctest 14/14 로그 (build/ctest_full_verbose.log)
  - 커버리지 스크린샷 (build/coverage_html/index.html)
  - 회귀 포인트: TC-B-02 중립, CSV text, main 키워드

리뷰 후 Phase 2 REFACTOR 브랜치 분리 (GREEN 유지)
```

### 변경·신규 파일 목록 (본 세션)

```
build/
├── ctest_full_verbose.log          # ctest -V 전체 로그
├── coverage_html/index.html        # gcovr HTML
└── coverage_baseline_phase1.info # baseline 복사본

build_cov/                          # 커버리지 전용 빌드 디렉터리
coverage_baseline_phase1.info       # gcovr lcov baseline

docs/
└── defect_list.md                  # H-1~H-3 Fixed, GREEN Phase

README.md                           # §RED To-Do [x], 커버리지 미달 명시

Report/Green/
└── 11.Green_Phase1_검증완료.md

Prompting/Green/
└── 11.Green_Phase1_검증완료-Prompt.md   ← 본 문서
```

---

## 변경 이력

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-22 | Turn 1~3 대화형 프롬프트 Export (GREEN Phase 1 검증 세션) |
