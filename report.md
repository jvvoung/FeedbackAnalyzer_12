# Prompting BP 회고

| 항목 | 내용 |
|------|------|
| **프로젝트** | Feedback Analyzer (C++17 리팩토링 챌린지) |
| **과정** | VSCode C++ · Java · Python 개발자 과정 — 2주·3주차 |
| **작성일** | 2026-05-22 |
| **실습 도구** | Cursor AI (Composer · Chat · @Codebase) |

---

## 1. Keep

> 실습에서 효과적이었고 앞으로도 유지할 것들

- **@Codebase 코드 탐색** — 전체 프로젝트 구조(디렉터리·의존성 그래프)를 10분 내 파악, 기존 수동 분석 대비 시간 90% 단축
- **Composer(Ctrl+I) 리팩토링 제안** — God Class·SRP 위반 클래스를 즉시 분리 (`main.cpp` 372줄 → 20줄 bootstrap)
- **`/test` 테스트 자동 생성** — `@파일명 /test` 한 줄로 Google Test / JUnit 5 경계값 케이스 자동 작성, 커버리지 40% → 80%+ 달성
- **Doxygen / JSDoc 주석 일괄 추가** — 문서화 시간 95% 절감, 보고서(`review_report.md`) 자동 생성
- **`@파일명` 명시적 컨텍스트 참조** — 관련 소스만 정확히 주입해 할루시네이션 빈도 감소
- **빌드·테스트 검증 요청 포함** — 프롬프트 마지막에 "구현 후 `ctest --output-on-failure` 실행" 명시 → 즉각 검증 루프 정착

---

## 2. Problem

> 실습 중 발생한 문제점과 한계

- **AI 할루시네이션** — 없는 STL 메서드 참조, C++14/17 혼동으로 컴파일 에러 발생 → 매번 빌드 검증 필요
- **컨텍스트 손실** — 긴 대화 후 초기 요구사항 망각, 대용량 헤더 참조 시 컨텍스트 초과 → 분할 참조 전략 필요
- **모호한 프롬프트** — "리뷰해줘" 수준 → 표면적 분석만 반환, SOLID·코드스멜 관점 분석 미흡
- **`Accept All` 남발** — 의도치 않은 파일 수정, `CMakeLists.txt` · `requirements.txt` 자동 변경으로 빌드 충돌
- **언어·버전 미명시** — C++ 표준, 빌드 도구 버전 누락 시 호환 불가 코드 생성

---

## 3. Try

> 다음 실습 · 현업에서 개선해서 시도할 것들

### P-C-T-F 구조 프롬프트 적용

| 구성요소 | 내용 |
|---------|------|
| **P** (역할) | "당신은 10년 경력 시니어 C++17 아키텍트입니다." |
| **C** (맥락) | `@RouteHandlers.cpp` — ECB 패턴, httplib, C++17, CMake 빌드 환경 |
| **T** (작업) | SOLID·코드스멜·테스트 가능성 관점으로 리뷰, Critical/Major/Minor 분류 |
| **F** (출력 형식) | Before/After 코드 블록 + Markdown 표, 수정 후 `ctest --output-on-failure` 실행 |

### 추가 개선 방안

- **할루시네이션 방지** — 프롬프트에 "구현 후 즉시 빌드·테스트 실행, 에러 시 수정" 명시
- **컨텍스트 유지** — 30분마다 핵심 요구사항 재주입, `.cursorrules` / `CLAUDE.md` 상시 로드
- **Diff 필수 확인** — `Accept All` 금지, 파일별 개별 Diff 검토 후 수락 규칙화
- **`.cursorrules` 등록** — Keep 항목·P-C-T-F 템플릿을 팀 공용 규칙으로 등록 → 다음 프로젝트 자동 적용

---

## 4. 개인별 학습

> 이번 실습을 통해 개인적으로 배운 점

- **좋은 프롬프트 작성법** — P-C-T-F 4단계 구조로 역할·맥락·작업·출력 형식을 명확히 정의할수록 결과 품질이 비례해서 향상됨
- **AI 결과 검증의 중요성** — 생성 코드를 맹신하지 않고 빌드·테스트를 통해 즉각 검증하는 습관이 핵심
- **TDD 사이클 속도 향상** — RED(`/test` 자동 생성) → GREEN(AI 구현 제안) → Refactoring(Composer 분리) 사이클을 AI로 단축 가능
- **@Codebase 활용 코드 리뷰 관점** — 전체 구조를 파악한 후 특정 파일을 리뷰하면 맥락 오류 없이 정확한 피드백을 받을 수 있음
- **테스트 자동화 가치 재확인** — AI가 생성한 경계값 테스트가 실제 결함(H-1~H-3)을 사전에 탐지함으로써 테스트의 실용적 가치 체감
- **언어·버전 명시의 중요성** — C++17 표준, CMake 버전, 빌드 도구를 프롬프트에 명시해야 호환성 문제를 예방할 수 있음

---

## 5. 팀 액션 아이템

| 영역 | 액션 | 책임자 | 기한 | 성공 기준 |
|------|------|--------|------|-----------|
| **Prompting** | 팀 공용 `.cursorrules` 작성 (P-C-T-F + 언어별 버전 규칙) | 팀장 | 1주 | 모든 PR에서 일관된 AI 응답 확인 |
| **테스트 품질** | 커버리지 80% 미달 PR Fail 규칙 CI 추가 | DevOps 담당 | 2주 | `main` 브랜치 커버리지 80%+ 유지 |
| **코드 리뷰** | Cursor AI 리뷰 결과를 PR 코멘트로 자동화 | 전체 | 3주 | 수동 리뷰 시간 50% 단축 |
| **문서화** | Doxygen 주석 일괄 생성 스크립트 공유 및 규칙 정리 | 팀원 A | 1개월 | 전체 public API 100% 문서화 |
| **레거시 개선** | 레거시 모듈 1개 선정 → AI 지원 리팩토링 시범 적용 | 팀원 B | 3개월 | 코드 스멜 50%↓ / 커버리지 70%↑ |

---

## 6. 현업 적용 계획

### 1개월 — 즉시 적용 (도구 정착)

- 팀 공용 `.cursorrules` 작성 완료 및 Git 저장소 등록
- Cursor AI를 활용한 일일 코드 리뷰 습관 형성
- 기존 프로젝트 1개 모듈을 `@Codebase`로 분석하여 구조 파악
- Google Test + CMake CI 파이프라인 구축 (커버리지 측정 포함)

### 3개월 — 확장 (팀 프로세스 통합)

- 팀 Prompting BP 가이드 문서 v1.0 작성 및 내부 공유
- 레거시 모듈 1~2개 AI 지원 리팩토링 완료
- 커버리지 80%+ CI 게이트 전체 적용 및 PR 차단 규칙 운영
- 사내 AI 도구 활용 세미나 발표 1회 진행

### 6개월 — 내재화 (조직 문화 정착)

- AI 활용 성과 정량화 (개발 시간 단축·버그 감소율 측정)
- 신규 팀원 온보딩 자료에 AI 활용 가이드 포함
- 팀 AI 활용 Best Practice를 사내 전체 팀에 공유
- MCP 연동으로 GitHub Issues → 코드 → PR 자동화 파이프라인 구축

---

> **참고 교재:** Prompting BP — Cursor AI 실습 회고 & 현업 적용 (C++17 · Java 21 · Python 3.12)
