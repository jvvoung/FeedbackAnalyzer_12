# Feedback Analyzer — 프로젝트 진행 보고서 (Feature·SentimentClassifier 가중치 스코어링)

| 항목 | 내용 |
|------|------|
| 프로젝트 | Feedback Analyzer (C++17 리팩토링 챌린지) |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| 저장소 | https://github.com/jvvoung/FeedbackAnalyzer_12.git |
| 보고 일자 | 2026-05-22 |
| 보고 범위 | **연습 #4 — 신규 기능 — 감성 분석 정확도 개선** (first-match → 가중치 스코어링) |
| 선행 보고서 | [`20.Feature_FileHandler_SaveToCsv_진행완료.md`](20.Feature_FileHandler_SaveToCsv_진행완료.md), [`17.Refactoring_Exercise4_NamingDesign_진행완료.md`](../Refactoring/17.Refactoring_Exercise4_NamingDesign_진행완료.md) |
| 근거 | `docs/PRD.md` §3.2 F-06, C-01, `docs/analysis.md` §2.1.2 (H-1), `.cursorrules` §3 |

---

## 1. Executive Summary

본 세션에서는 교육 슬라이드 **「연습 #4 — 신규 기능 — 감성 분석 정확도 개선」**을 Feedback Analyzer Domain 레이어에 TDD로 적용하였다. 기존 **first-match** 감성 판정(긍정 키워드 선매칭 → 부정 → 중립 fallback)을 **가중치 기반 스코어링**(`SentimentScore` 집계 후 `positive` vs `negative` 비교)으로 교체하였다.

| 구분 | Report 20 (선행) | 본 세션 (Feature) |
|------|------------------|-------------------|
| `SentimentClassifier` | first-match `containsAny` if-else | **`SentimentScore` + `classifyWeighted()`** |
| `classify()` | 긍정 → 부정 → 중립 순차 매칭 | **`classifyWeighted()` 위임** (단일 진입점) |
| PRD F-06 | first-match 순서 규칙 | **가중치 스코어링 규칙** 개정 |
| EX-09 | 혼합 감성 → **긍정 우선** | 혼합 감성 동점 → **중립** (의도적 교체) |
| TextAnalyzer·Filters | `FeedbackClassifier` 경유 | ✅ **변경 없음** (Domain 교체만) |
| HTTP Boundary | 5엔드포인트 불변 | ✅ **변경 없음** |
| ctest | 25/25 Pass (1 Skipped) | ✅ **32/32 Pass** (1 Skipped, **+7 SentimentClassifierTest**) |
| Golden Master GM-D-01~04 | 4/4 Pass | ✅ **4/4 Pass** (baseline 미갱신) |
| `httplib.h` diff | 0 | ✅ **0** |

**핵심 판정:** Domain 감성 분류 알고리즘을 슬라이드 To-Be API대로 업그레이드하였으며, `TextAnalyzer`·`Filters`는 `SentimentClassifier` 단일 소스를 유지한다. Golden Master 시나리오에 혼합 감성 동점 케이스가 없어 baseline 갱신은 불필요하다.

---

## 2. 세션 배경 및 목적

### 2.1 As-Is (시작 조건)

| 항목 | 상태 |
|------|------|
| `SentimentClassifier::classify()` | `KeywordUtils::containsAny` first-match (긍→부→중립) |
| `FeedbackClassifier::classifySentiment()` | `SentimentClassifier::classify()` 위임 |
| `TextAnalyzer` / `Filters` | `FeedbackClassifier` 경유 (F-06 단일 소스) |
| PRD F-06 | 긍정 키워드 매칭 → 부정 → 중립 fallback |
| test_plan EX-09 | 혼합 감성 `"좋아요 but 나쁘기도 해요"` → **긍정** |
| ctest | 25/25 Pass, Golden Master 4/4 Pass |

### 2.2 슬라이드 To-Be (목표 API)

```cpp
struct SentimentScore { int positive{0}; int negative{0}; };

// 긍정 키워드 전체 카운트 → score.positive++
// 부정 키워드 전체 카운트 → score.negative++
// positive > negative → "긍정"
// negative > positive → "부정"
// 동점(0 포함)     → "중립"
string classifyWeighted(const string& text);
```

### 2.3 계약 충돌 및 해결

| 충돌 | 내용 | 본 세션 조치 |
|------|------|-------------|
| PRD F-06 vs 슬라이드 | first-match vs 가중치 스코어링 | **선택 B** — `classifyWeighted()` 추가 + `classify()` 위임, **PRD F-06 개정** |
| EX-09 vs SC-W06 | 혼합 1:1 → 긍정 우선 vs **중립** | PRD 개정에 따라 **EX-09 테스트 의도적 수정** |
| GM-D-01~04 | baseline 고정 | 혼합 동점 미포함 → **Pass 유지, baseline 미갱신** |

### 2.4 세션 목표

| # | 목표 | 산출물 |
|---|------|--------|
| 1 | TDD RED — SentimentClassifierTest SC-W01~06 | `tests/SentimentClassifierTest.cpp` |
| 2 | TDD GREEN — `SentimentScore`, `scoreWeighted`, `classifyWeighted` | `src/cpp/SentimentClassifier.h` |
| 3 | 통합 — `classify()` → `classifyWeighted()` 위임 | `SentimentClassifier.h` |
| 4 | PRD F-06 가중치 규칙 반영 | `docs/PRD.md` |
| 5 | EX-09 테스트 교체 | `tests/TextAnalyzerTest.cpp` |
| 6 | 회귀 Gate | ctest 32/32 + Golden Master 4/4 |

### 2.5 작업 제약 (준수 여부)

| 제약 | 내용 | 준수 |
|------|------|------|
| `httplib.h` diff 0 | 외부 라이브러리 미수정 | ✅ |
| HTTP 5엔드포인트 path/method 불변 | PRD §7.2 | ✅ |
| Domain 단일 소스 | `classifyWeighted`를 Filters·TextAnalyzer에 복사 금지 | ✅ |
| 카테고리 키워드 혼입 금지 | T-07 quality≠sentiment | ✅ |
| Golden Master 무분별 갱신 금지 | `domain_golden_expected.txt` 유지 | ✅ |
| 감정 라벨 고정 | u8"긍정"\|u8"부정"\|u8"중립" (C-01) | ✅ |

---

## 3. TDD 진행 (RED → GREEN → REFACTOR)

### 3.1 Step 0 — Baseline Gate

```powershell
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build --output-on-failure
```

| 항목 | 결과 |
|------|------|
| ctest | **25/25 Pass**, 0 Failed, 1 Skipped |
| GoldenMaster GM-D-01~04 | **4/4 Pass** |

### 3.2 Step 1 — RED (`tests/SentimentClassifierTest.cpp`)

| ID | Given | When | Then |
|----|-------|------|------|
| **SC-W01** | 긍정 키워드만 (`"좋아요 만족"`) | `classifyWeighted` | `"긍정"`, positive>negative |
| **SC-W02** | 부정 키워드만 (`"나쁘 불만"`) | `classifyWeighted` | `"부정"` |
| **SC-W03** | 감정 키워드 없음 (`"그냥 그래요"`) | `classifyWeighted` | `"중립"` (T-03) |
| **SC-W04** | 긍정2 + 부정1 혼합 | `classifyWeighted` | `"긍정"` (가중치 우세) |
| **SC-W05** | 긍정1 + 부정2 혼합 | `classifyWeighted` | `"부정"` |
| **SC-W06** | 긍정1 + 부정1 동점 | `classifyWeighted` | `"중립"` |
| **SC-W01b** | 긍정 키워드만 | `classify()` | `classifyWeighted()`와 동일 |

> RED 단계: first-match `classify()` 구현 상태에서 SC-W04~06·SC-W01b Fail 예상 → GREEN 구현 후 Pass.

### 3.3 Step 2 — GREEN (`SentimentClassifier.h`)

```cpp
struct SentimentScore {
    int positive{0};
    int negative{0};
};

static SentimentScore scoreWeighted(const std::string& text) {
    SentimentScore score;
    for (const auto& kw : Constants::SENTIMENT_KEYWORDS.at(u8"긍정")) {
        if (text.find(kw) != std::string::npos) score.positive++;
    }
    for (const auto& kw : Constants::SENTIMENT_KEYWORDS.at(u8"부정")) {
        if (text.find(kw) != std::string::npos) score.negative++;
    }
    return score;
}

static std::string classifyWeighted(const std::string& text) {
    const SentimentScore score = scoreWeighted(text);
    if (score.positive > score.negative) return u8"긍정";
    if (score.negative > score.positive) return u8"부정";
    return u8"중립";
}

static std::string classify(const std::string& text) {
    return classifyWeighted(text);
}
```

| 슬라이드 요건 | 구현 |
|--------------|------|
| `SentimentScore { positive, negative }` | ✅ |
| 키워드 소스 `SENTIMENT_KEYWORDS[u8"긍정"]`, `[u8"부정"]` ONLY | ✅ |
| 키워드별 매칭 건수 집계 (리스트 순회, 매칭 시 ++) | ✅ |
| positive > negative → `"긍정"` | ✅ |
| negative > positive → `"부정"` | ✅ |
| 동점(0 포함) → `"중립"` | ✅ |
| `classify()` → `classifyWeighted()` 위임 | ✅ |

### 3.4 Step 3 — REFACTOR (단일 소스 확인)

| 호출 경로 | 위임 관계 | 변경 |
|-----------|-----------|------|
| `TextAnalyzer` | `FeedbackClassifier::classifySentiment()` → `SentimentClassifier::classify()` | ❌ 코드 변경 없음 (자동 반영) |
| `Filters` | `FeedbackClassifier::classifySentiment()` → `SentimentClassifier::classify()` | ❌ 코드 변경 없음 |
| `FeedbackClassifier.h` | `return SentimentClassifier::classify(text);` | ❌ 변경 없음 |

**아키텍처 (1~2줄):** `classifyWeighted()`는 `SentimentClassifier`에만 존재하며, `FeedbackClassifier` 허브를 통해 `TextAnalyzer`·`Filters`가 동일 규칙을 공유한다. Boundary(HTML/HTTP) 레이어는 수정하지 않았다.

### 3.5 EX-09 테스트 의도적 수정

**Before (first-match):**

```cpp
TEST_F(TextAnalyzerTest, Given_MixedPositiveAndNegativeKeywords_When_AnalyzeSentiment_Then_PositiveFirst) {
    // "좋아요 but 나쁘기도 해요" → 긍정=1, 부정=0, 중립=0
}
```

**After (weighted):**

```cpp
TEST_F(TextAnalyzerTest, Given_MixedPositiveAndNegativeKeywords_When_AnalyzeSentiment_Then_NeutralOnTie) {
    // "좋아요"(+1) + "나쁘"(+1) → 동점 → 중립=1
}
```

---

## 4. 변경 파일 목록

| 파일 | 변경 유형 | 설명 |
|------|-----------|------|
| `src/cpp/SentimentClassifier.h` | 수정 | `SentimentScore`, `scoreWeighted`, `classifyWeighted`; `classify()` 위임 |
| `tests/SentimentClassifierTest.cpp` | **신규** | SC-W01~06 + SC-W01b (7 tests) |
| `tests/TextAnalyzerTest.cpp` | 수정 | EX-09 — 동점 중립 기대값으로 교체 |
| `docs/PRD.md` | 수정 | F-06 first-match → 가중치 스코어링 |
| `CMakeLists.txt` | 수정 | tests 타깃에 `SentimentClassifierTest.cpp` 추가 |

**미변경 (범위 외):**

| 파일 | 사유 |
|------|------|
| `src/cpp/TextAnalyzer.h` | `FeedbackClassifier` 경유 — Domain 교체로 자동 반영 |
| `src/cpp/Filters.h` | 동일 |
| `src/cpp/FeedbackClassifier.h` | `classifySentiment()` 시그니처·위임 유지 |
| `src/cpp/Constants.cpp` | 감성 키워드 목록 불변 |
| `src/cpp/httplib.h` | diff 0 |
| `tests/golden/domain_golden_expected.txt` | GM 시나리오에 혼합 동점 없음 — baseline 유지 |

---

## 5. ctest · Golden Master 결과

### 5.1 실행 명령

```powershell
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build
cmake --build c:\DEV\FeedbackAnalyzer_12\build
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build --output-on-failure
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build -R "SentimentClassifier|TextAnalyzerTest|FiltersTest|GoldenMaster" -V
```

### 5.2 ctest 스냅샷 (2026-05-22)

| 항목 | Report 20 | 본 세션 |
|------|-----------|---------|
| 총 테스트 | 25 | **32** (+7) |
| 통과 | 24 (discovered) + GoldenMaster label | **31 (discovered) + GoldenMaster label** |
| 실패 | 0 | **0** |
| Skipped | 1 | **1** (`GoldenMasterCapture.UpdateGoldenFile_WhenGoldenUpdateEnvSet`) |
| 판정 | GREEN | ✅ **GREEN** |

**신규 Pass (7건):**

| # | 테스트 | 시나리오 ID |
|---|--------|-------------|
| 1 | `SentimentClassifierTest.SC_W01_Given_PositiveKeywordsOnly_When_ClassifyWeighted_Then_Positive` | SC-W01 |
| 2 | `SentimentClassifierTest.SC_W02_Given_NegativeKeywordsOnly_When_ClassifyWeighted_Then_Negative` | SC-W02 |
| 3 | `SentimentClassifierTest.SC_W03_Given_NoSentimentKeywords_When_ClassifyWeighted_Then_Neutral` | SC-W03 |
| 4 | `SentimentClassifierTest.SC_W04_Given_TwoPositiveOneNegative_When_ClassifyWeighted_Then_Positive` | SC-W04 |
| 5 | `SentimentClassifierTest.SC_W05_Given_OnePositiveTwoNegative_When_ClassifyWeighted_Then_Negative` | SC-W05 |
| 6 | `SentimentClassifierTest.SC_W06_Given_EqualPositiveAndNegative_When_ClassifyWeighted_Then_Neutral` | SC-W06 |
| 7 | `SentimentClassifierTest.SC_W01b_Given_PositiveKeywordsOnly_When_Classify_Then_DelegatesToWeighted` | SC-W01b |

**수정 Pass (1건):**

| 테스트 | 변경 |
|--------|------|
| `TextAnalyzerTest.Given_MixedPositiveAndNegativeKeywords_When_AnalyzeSentiment_Then_NeutralOnTie` | EX-09 — 긍정 우선 → 동점 중립 |

**Golden Master:**

| ID | 테스트 | 결과 | baseline |
|----|--------|------|----------|
| GM-D-01 | AnalyzeNegativeDelivery | ✅ Pass | 변경 없음 |
| GM-D-02 | FilterNeutral | ✅ Pass | 변경 없음 |
| GM-D-03 | CsvParseTextColumn | ✅ Pass | 변경 없음 |
| GM-D-04 | FilterAll | ✅ Pass | 변경 없음 |
| GoldenMaster (label) | `--gtest_filter=GoldenMasterTest.*` | ✅ Pass | — |

---

## 6. EX-09 / GM-D-xx 영향 분석

### 6.1 Before vs After (first-match → weighted)

| 케이스 | Before (first-match) | After (weighted) | GM baseline |
|--------|---------------------|------------------|-------------|
| **EX-09** `"좋아요 but 나쁘기도 해요"` | **긍정** (긍정 키워드 선매칭) | **중립** (positive=1, negative=1 동점) | N/A |
| **SC-W04** `"좋아요 만족 but 나쁘"` | **긍정** (first-match) | **긍정** (2>1, 동일 결과) | N/A |
| **SC-W05** `"좋아요 but 나쁘 불만"` | **긍정** (first-match) | **부정** (1<2, **결과 변경**) | N/A |
| **GM-D-01** 부정 배송 텍스트 | 부정 1 | 부정 1 | ✅ |
| **GM-D-02** 중립 필터 | 중립 1건 | 중립 1건 | ✅ |
| **GM-D-03** CSV 파싱 | count=1 | count=1 | ✅ |
| **GM-D-04** 전체 필터 3건 | 긍/부/중립 각 1 | 긍/부/중립 각 1 | ✅ |

> **주의:** SC-W05 유형(긍정1+부정2)은 first-match에서는 **긍정**이었으나 weighted에서는 **부정**으로 변경된다. GM-D-04의 3건(`좋아요 만족합니다` / `배송이 너무 늦어요` / `그냥 그래요`)은 단일 감성 키워드만 포함하므로 결과 불변.

### 6.2 Domain 호출 흐름 (불변)

```
TextAnalyzer / Filters
        ↓
FeedbackClassifier::classifySentiment()
        ↓
SentimentClassifier::classify()  ← classifyWeighted() 위임
        ↓
Constants::SENTIMENT_KEYWORDS[u8"긍정"|u8"부정"]
```

---

## 7. PRD F-06 개정 내용

**Before:**

| 규칙 | 내용 |
|------|------|
| 감정 판정 순서 | 긍정 키워드 매칭 → 부정 키워드 매칭 → 그 외 **중립** |

**After:**

| 규칙 | 내용 |
|------|------|
| 감정 판정 (가중치 스코어링) | `SENTIMENT_KEYWORDS[u8"긍정"]`·`[u8"부정"]` 키워드별 등장 건수 집계 → `positive`/`negative` 비교: `positive > negative` → **긍정**, `negative > positive` → **부정**, 동점(0 포함) → **중립** (`SentimentClassifier::classifyWeighted`) |

---

## 8. 잔여 항목 (별도 PR 권장)

| 순위 | 항목 | 근거 | 우선순위 |
|------|------|------|----------|
| 1 | `docs/analysis.md` §2.1.2 first-match 서술 → weighted 동기화 | PRD F-06과 문서 일치 | P2 |
| 2 | `README.md` 감성 분류 규칙 표 갱신 | first-match → weighted | P2 |
| 3 | SC-W07 — TextAnalyzer 3건 혼합 + Filters 중립 subset AC-1 확장 테스트 | test_plan | P3 |
| 4 | `SentimentClassifier` 커버리지 측정 (`build_cov`) | AC-4 | P2 |
| 5 | Git 커밋 | 워킹 트리 변경 | P1 |

---

## 9. 종합 판정

| Gate | 판정 |
|------|------|
| 슬라이드 To-Be API (`SentimentScore`, `classifyWeighted`) | ✅ **달성** |
| TDD SC-W01~06 + SC-W01b | ✅ **7/7 Pass** |
| `classify()` 단일 진입점 위임 | ✅ **달성** |
| PRD F-06 가중치 규칙 개정 | ✅ **달성** |
| EX-09 의도적 수정 (동점 → 중립) | ✅ **달성** |
| ctest 전체 | ✅ **32/32 Pass** (1 Skipped) |
| Golden Master GM-D-01~04 | ✅ **4/4 Pass** |
| HTTP·Boundary | ✅ **불변** |
| `httplib.h` diff | ✅ **0** |
| Git 커밋 | ⏳ **미커밋** (워킹 트리 변경) |

**롤백:** 불필요. ctest 0 failures, Golden Master diff 없음.

### 9.1 권장 커밋 메시지 (참고)

```text
feat(domain): add weighted sentiment scoring (classifyWeighted)

Replace first-match with SentimentScore count compare.
Update PRD F-06; SentimentClassifierTest SC-W01~06. Golden Master baseline unchanged.
```

---

## 10. 변경 이력

| 일자 | Phase | 변경 내용 |
|------|-------|-----------|
| 2026-05-22 | Step 0 | ctest 25/25 Green, Golden Master 4/4 baseline 확인 |
| 2026-05-22 | RED | `SentimentClassifierTest.cpp` SC-W01~06 작성, CMakeLists tests 타깃 추가 |
| 2026-05-22 | GREEN | `SentimentClassifier.h` — `SentimentScore`, `scoreWeighted`, `classifyWeighted` |
| 2026-05-22 | 통합 | `classify()` → `classifyWeighted()` 위임; EX-09 테스트 교체 |
| 2026-05-22 | PRD | F-06 first-match → 가중치 스코어링 개정 |
| 2026-05-22 | 검증 | ctest 32/32 Pass, GM 4/4 Pass |
| 2026-05-22 | 보고서 | 본 문서 작성 — `Report/Feature/21.*` |

---

*본 문서는 교육 슬라이드 **「연습 #4 — 감성 분석 정확도 개선」** 세션의 공식 기록이다. Domain 레이어(`SentimentClassifier`)만 교체하였으며, HTTP Boundary와 Golden Master baseline은 변경하지 않는다.*
