# Feedback Analyzer — 프로젝트 진행 보고서 (Refactoring·Phase 3 네이밍·상수·레거시 정리)

| 항목 | 내용 |
|------|------|
| 프로젝트 | Feedback Analyzer (C++17 리팩토링 챌린지) |
| 워크스페이스 | `c:\DEV\FeedbackAnalyzer_12` |
| 저장소 | https://github.com/jvvoung/FeedbackAnalyzer_12.git |
| 보고 일자 | 2026-05-22 |
| 보고 범위 | **Phase 3·L-5** — 네이밍·매직넘버 개선 + Report 17 잔여 스멜 정리 + 슬라이드 6대 레거시 최종 검증 |
| 선행 보고서 | [`17.Refactoring_Exercise4_NamingDesign_진행완료.md`](17.Refactoring_Exercise4_NamingDesign_진행완료.md) |
| 근거 | `docs/analysis.md` (L-1, M-2, M-4, M-5, M-6), `docs/PRD.md` §9 (L-3, L-5), `docs/defect_list.md`, 교육 슬라이드 연습 #4 개요 |

---

## 1. Executive Summary

본 세션에서는 Report 17 이후 남아 있던 **코드 스멜 진단 → 네이밍·매직넘버 중심 리팩토링 → 잔여 레거시 정리**를 수행하였다. Report 17 §8 잔여 5건 중 **4건을 코드로 해소**하고, 교육 슬라이드 **6대 레거시 문제를 전부 Resolved**로 확인하였다.

| 구분 | Report 17 (선행) | 본 세션 (구현 후) |
|------|------------------|-------------------|
| `TextAnalyzer::globalSent/Kw` | ⚠️ static 잔존 | ✅ **삭제** (반환값만 사용) |
| `Filters::filter()` `std::cout` | ⚠️ 부작용 잔존 | ✅ **제거** |
| `FileHandler.h` dead code | ⚠️ 미참조 | ✅ **삭제** |
| 매직넘버 (`8080`, BOM, `전체`) | ⚠️ 인라인 | ✅ **ServerConfig / CsvConstants / FilterConstants** |
| `UIComponents` Lazy Class | ⚠️ 존재 | ✅ **삭제** → `KeywordRegistry` 직접 사용 |
| `AppState` 직접 접근 | ⚠️ RouteHandlers·FilterUseCase 혼재 | ✅ **FeedbackSession API 통합** |
| CSV 필드 이스케이프 | ❌ 없음 | ✅ **`CsvExporter::escapeCsvField`** |
| 슬라이드 6대 레거시 | ⚠️ #4·#6 잔존 | ✅ **6/6 Resolved** |
| ctest 전체 | 20/20 Pass | ✅ **20/20 Pass** (1 Skipped) |
| Golden Master GM-D-01~04 | 4/4 Pass | ✅ **4/4 Pass** (baseline 미갱신) |
| `httplib.h` diff | 0 | ✅ **0** |

**워킹 트리 diff (HEAD 대비):** 13 files, **+76 / −98** lines (신규 상수 헤더 3건, 삭제 4건).

---

## 2. 세션 배경 및 목적

### 2.1 시작 조건 (Report 17 이후)

| 항목 | 상태 |
|------|------|
| 연습 #4 Rename (`sent`/`kw`/`fil`) | ✅ 완료 |
| `FeedbackSession` 도입 | ✅ 완료 |
| `KeywordUtils` `std::any_of` | ✅ 완료 |
| Report 17 §8 잔여 | `globalSent/Kw`, Filters `cout`, FileHandler, defect_list stale, CsvParser 커버리지 |

### 2.2 세션 목표

| # | 목표 | 산출물 |
|---|------|--------|
| 1 | 매직넘버·하드코딩 상수 추출 | `ServerConfig.h`, `CsvConstants.h`, `FilterConstants.h` |
| 2 | 전역 static 캐시 제거 | `TextAnalyzer.h` — `globalSent`/`globalKw` 삭제, header-only |
| 3 | 도메인 부작용 제거 | `Filters.h` — `std::cout` 루프 삭제 |
| 4 | Dead Code·Lazy Class 정리 | `FileHandler.h`, `UIComponents.*`, `TextAnalyzer.cpp` 삭제 |
| 5 | 상태 접근 API 일원화 | `FeedbackSession::getLastFiltered` / `setLastFiltered` |
| 6 | CSV 품질 개선 | `CsvExporter` 필드 이스케이프 (쉼표·따옴표·개행) |
| 7 | 슬라이드 6대 레거시 최종 검증 | Grep + ctest Green |

### 2.3 작업 제약 (준수 여부)

| 제약 | 내용 | 준수 |
|------|------|------|
| Golden Master 무분별 갱신 금지 | `domain_golden_expected.txt` 유지 | ✅ |
| `httplib.h` diff 0 | 외부 라이브러리 미수정 | ✅ |
| HTTP·CSV·HTML 계약 | 5엔드포인트·BOM+`text\n`·포트 8080 불변 | ✅ |
| 테스트 삭제·완화·skip 추가 금지 | 20 Pass / 0 Fail 유지 | ✅ |

---

## 3. 구현 상세

### 3.1 Part C-1 — 상수 모듈 추출 (매직넘버 개선)

#### `ServerConfig.h` (신규)

```cpp
namespace ServerConfig {
inline constexpr const char* kBindHost = "0.0.0.0";
inline constexpr int kPort = 8080;
inline std::string publicUrl();  // http://localhost:8080
}
```

| Before | After |
|--------|-------|
| `main.cpp` — `"0.0.0.0"`, `8080`, URL 문자열 하드코딩 | `ServerConfig::kBindHost`, `kPort`, `publicUrl()` |

#### `CsvConstants.h` (신규)

| 상수 | 값 | 용도 |
|------|-----|------|
| `kUtf8Bom` | `\xEF\xBB\xBF` | Excel UTF-8 인식 |
| `kTextColumnHeader` | `text\n` | PRD §7.2 CSV 헤더 |
| `kAttachmentFilename` | `filtered_feedback.csv` | Content-Disposition |
| `kContentType` | `text/csv; charset=UTF-8` | MIME |

#### `FilterConstants.h` (신규)

| 상수 | 값 | 용도 |
|------|-----|------|
| `kAllSentinel` | `u8"전체"` | 감정·키워드 필터 «전체» sentinel |

`Filters.h`의 `u8"전체"` 문자열 리터럴 2곳 → `FilterConstants::kAllSentinel` 치환.

---

### 3.2 Part C-2 — `TextAnalyzer` 전역 static 제거

**Before** (`TextAnalyzer.h` + `TextAnalyzer.cpp`):

```cpp
static std::map<std::string, int> globalSent;
static std::map<std::string, int> globalKw;
// analyzeSentiment/analyzeKeywords 내부에서 globalSent/Kw = res; (쓰기만, 읽기 없음)
```

**After**:

- static 멤버 **완전 삭제**
- `TextAnalyzer.cpp` **삭제** (구현은 헤더 전용)
- 지역 변수명 개선: `sentimentCounts`, `keywordCounts`, `feedback`, `text`, `category`
- `Constants.h` include 제거 (미사용)

| 항목 | 결과 |
|------|------|
| `globalSent` / `globalKw` Grep | ✅ 0건 |
| `TextAnalyzerTest` 4건 | ✅ Pass |
| Golden Master GM-D-01 | ✅ Pass |

---

### 3.3 Part C-3 — `Filters` 부작용·네이밍 개선

**Before**:

```cpp
for (const auto& i : finalFiltered) {
    std::cout << i.getText() << std::endl;  // DEF-007
}
```

**After**:

- `std::cout` 루프 **삭제**, `#include <iostream>` 제거
- 파라미터 Rename: `sFilter`/`kFilter` → `sentimentFilter`/`keywordFilter`
- 중간 변수 Rename: `tmpFiltered`/`finalFiltered` → `sentimentFiltered`/`keywordFiltered`

| 항목 | 결과 |
|------|------|
| DEF-007 Filters cout | ✅ **Resolved** |
| `FiltersTest` 4건 | ✅ Pass |

---

### 3.4 Part C-4 — Dead Code·Lazy Class 제거

| 파일 | 조치 | 근거 |
|------|------|------|
| `FileHandler.h` | **삭제** | 프로젝트 전체 참조 0건; `saveResult()` cout 스텁 (슬라이드 #6) |
| `UIComponents.h` / `UIComponents.cpp` | **삭제** | `KeywordRegistry::categoryNames()` 1줄 래핑만 수행 |
| `HtmlPageRenderer.cpp` | `KeywordRegistry` 직접 include | Lazy Class 제거 |
| `CMakeLists.txt` | `UIComponents.cpp`, `TextAnalyzer.cpp` 제거 | 빌드 대상 정리 |

---

### 3.5 Part C-5 — `FeedbackSession` API 통합

Report 17에서 `RouteHandlers`는 `FeedbackSession::getCurrent()`, download는 `AppState::lastFilteredFeedbacksConst()` **직접 호출**로 이중화되어 있었다.

**After** (`FeedbackSession.h`):

```cpp
static const std::vector<Feedback>& getLastFiltered();
static void setLastFiltered(const std::vector<Feedback>& filtered);
static void clear();  // current + lastFiltered 동시 clear
```

| 호출부 | Before | After |
|--------|--------|-------|
| `FilterUseCase.cpp` | `AppState::setLastFilteredFeedbacks` | `FeedbackSession::setLastFiltered` |
| `RouteHandlers.cpp` (download) | `AppState::lastFilteredFeedbacksConst` | `FeedbackSession::getLastFiltered` |

`RouteHandlers.cpp`에서 `#include "AppState.h"` 제거.

---

### 3.6 Part C-6 — `CsvExporter` 필드 이스케이프

**Before**: `csv << feedback.getText() << "\n";` (쉼표·따옴표·개행 미처리)

**After**: RFC 4180 스타일 `escapeCsvField()` — `,` `"` `\n` `\r` 포함 시 따옴표 감싸기, 내부 `"` → `""`

| 계약 | 준수 |
|------|------|
| BOM + `text\n` 헤더 | ✅ `CsvConstants` 경유, 값 동일 |
| PRD R-3 | ✅ 헤더 형식 불변 |
| AC-7 개행 유지 | ✅ quoted field로 개행 보존 |

---

## 4. 변경 파일 목록

| 구분 | 파일 | 변경 유형 |
|------|------|-----------|
| Config | `src/cpp/ServerConfig.h` | **신규** |
| Config | `src/cpp/CsvConstants.h` | **신규** |
| Config | `src/cpp/FilterConstants.h` | **신규** |
| Domain | `src/cpp/TextAnalyzer.h` | 수정 — static 제거·네이밍 |
| Domain | `src/cpp/TextAnalyzer.cpp` | **삭제** |
| Domain | `src/cpp/Filters.h` | 수정 — cout 제거·상수·네이밍 |
| Domain | `src/cpp/FeedbackSession.h` | 수정 — getLastFiltered/setLastFiltered |
| Domain | `src/cpp/FilterUseCase.cpp` | 수정 — FeedbackSession 경유 |
| Boundary | `src/cpp/main.cpp` | 수정 — ServerConfig |
| Boundary | `src/cpp/CsvExporter.cpp` | 수정 — CsvConstants·escape |
| Boundary | `src/cpp/RouteHandlers.cpp` | 수정 — FeedbackSession 통합 |
| Boundary | `src/cpp/HtmlPageRenderer.cpp` | 수정 — KeywordRegistry 직접 |
| Infra | `src/cpp/FileHandler.h` | **삭제** |
| Infra | `src/cpp/UIComponents.h` | **삭제** |
| Infra | `src/cpp/UIComponents.cpp` | **삭제** |
| Build | `CMakeLists.txt` | 수정 — 소스 목록 정리 |

---

## 5. 빌드·ctest 실행

### 5.1 실행 명령 (`build_cov/`)

```powershell
cmake -S c:\DEV\FeedbackAnalyzer_12 -B c:\DEV\FeedbackAnalyzer_12\build_cov
cmake --build c:\DEV\FeedbackAnalyzer_12\build_cov
ctest --test-dir c:\DEV\FeedbackAnalyzer_12\build_cov --output-on-failure
```

### 5.2 ctest 결과 (2026-05-22, 구현 후)

| 항목 | Report 17 | 본 세션 |
|------|-----------|---------|
| 총 테스트 | 20 | 20 |
| 통과 | 20 | **20** |
| 실패 | 0 | **0** |
| Skip | 1 (`GoldenMasterCapture`) | **1** (정상) |

```text
100% tests passed, 0 tests failed out of 20
The following tests did not run:
  5 - GoldenMasterCapture.UpdateGoldenFile_WhenGoldenUpdateEnvSet (Skipped)
```

### 5.3 Golden Master (4/4)

| 섹션 | 테스트명 | 결과 |
|------|----------|------|
| GM-D-01 | `GM_D01_AnalyzeNegativeDelivery_MatchesGolden` | ✅ Pass |
| GM-D-02 | `GM_D02_FilterNeutral_MatchesGolden` | ✅ Pass |
| GM-D-03 | `GM_D03_CsvParseTextColumn_MatchesGolden` | ✅ Pass |
| GM-D-04 | `GM_D04_FilterAll_MatchesGolden` | ✅ Pass |

> `tests/golden/domain_golden_expected.txt` — **unified diff 없음** (baseline 미갱신).

### 5.4 P0 Gate (H-1~H-3)

| ID | 테스트 | 결과 |
|----|--------|------|
| H-1 / AC-1 | `FiltersTest.Given_NeutralText_When_FilterSentimentNeutral_Then_MatchesTextAnalyzerSet` | ✅ Pass (#13) |
| H-2 / AC-2 | `CsvParserTest.Given_CsvWithoutTextColumn_When_Parse_Then_ZeroFeedbacks` | ✅ Pass (#9) |
| H-3 / AC-3 | `FiltersTest.Given_MainKeywordOnly_When_FilterDelivery_Then_Included` | ✅ Pass (#15) |

---

## 6. 교육 슬라이드 6대 레거시 — 최종 체크리스트

> 슬라이드: **「연습 #4 — Feedback Analyzer 개요 & 레거시 문제점」**

| # | 슬라이드 문제 | Before | After (본 세션) | 상태 |
|---|--------------|--------|-----------------|------|
| 1 | 축약 메서드명 (`sent`, `kw`, `fil`, `fil_data`) | Report 17에서 Rename | Grep **0건**; `FeedbackSession::getLastFiltered` | ✅ **Resolved** |
| 2 | 중복 키워드 등록 (`Constants.cpp`) | Phase 1~2 해소 | 긍정 20·부정 23 고유, 교차 중복 없음 | ✅ **Resolved** |
| 3 | `containsAny()` 중복 | Report 17·Phase 2 해소 | `KeywordUtils.h` 단일 | ✅ **Resolved** |
| 4 | 전역 상태 (`fil_data`, `globalSent/Kw`) | `globalSent/Kw` 잔존 | static **삭제**; `fil_data` → `FeedbackSession` | ✅ **Resolved** |
| 5 | 의미 없는 Session | Report 17 `FeedbackSession` | `Session.h` 없음; key 무시 API 없음 | ✅ **Resolved** |
| 6 | FileHandler stub | `FileHandler.h` 잔존 | 파일 **삭제**; `/download` → `CsvExporter` | ✅ **Resolved** |

**판정: 슬라이드 6/6 Resolved.**

---

## 7. analysis.md · defect_list 추적

### 7.1 analysis.md 코드 스멜

| ID | 스멜 | Report 17 | 본 세션 |
|----|------|-----------|---------|
| **L-1** | 축약 메서드명 | ✅ Resolved | ✅ 유지 |
| **M-2** | 전역 상태 (`fil_data`, `globalSent/Kw`) | ⚠️ Partial | ✅ **Resolved** (명시 대상 제거) |
| **M-3** | `containsAny` 중복 | ✅ Resolved | ✅ 유지 |
| **M-4** | Logger/UI·Filters cout | ⚠️ Filters cout 잔존 | ✅ Filters cout **Resolved**; Logger UI는 Phase 4 |
| **M-5** | FileHandler Lava Flow | ❌ Open | ✅ **Resolved** |
| **M-6** | download 소스·CSV | ⚠️ Partial | ⚠️ **Partial** — API 통합·escape 추가; 필터 없이 download 빈 결과 정책 유지 |
| **M-7** | 키워드 이중 사전 | ✅ Resolved | ✅ 유지 |

> **M-2 잔여 (아키텍처):** `AppState`·`Logger::debugMode` 프로세스 전역 static은 Phase 5(DI/Repository) 과제.

### 7.2 defect_list.md (권장 갱신)

| ID | Report 17 | 본 세션 권장 상태 |
|----|-----------|-------------------|
| DEF-005 | Open (God main) | **Fixed** — `main.cpp` 18줄 (Phase 2) |
| DEF-006 | Open (전역 상태) | **Fixed** — `globalSent/Kw` 제거, `FeedbackSession` 통합 |
| DEF-007 | Open (Filters cout) | **Fixed** |
| DEF-008 | Open (download) | **Partial** — escape·API 개선; 정책(필터 후만) 유지 |
| DEF-009 | Open (키워드 불일치) | **Fixed** — Phase 1~2 + 본 세션 검증 |

> `docs/defect_list.md` 본문은 **아직 미갱신** (코드·본 보고서와 불일치).

---

## 8. 잔여 항목 (별도 PR 권장)

| 순위 | 항목 | 근거 | 우선순위 |
|------|------|------|----------|
| 1 | `docs/defect_list.md` 상태 갱신 | DEF-005~009 코드 반영 | P2 |
| 2 | CsvParser 커버리지 ≥90% | AC-4 (Report 16 미달 71%) | P0 |
| 3 | Logger ↔ UI level별 연동 | M-4 Phase 4 | P1 |
| 4 | `Constants.cpp` 미사용 서브키워드 (`time`, `type` 등) | YAGNI / Speculative Generality | P3 |
| 5 | `HtmlPageRenderer` 템플릿 분리 | Long Method | P3 |
| 6 | `.gitignore` Spring Boot 잔재 | L-4 | P3 |
| 7 | HTTP 부록 A 수동 스모크 | AC-5 | P1 |

---

## 9. 종합 판정

| Gate | 판정 |
|------|------|
| Phase 3 네이밍·상수·전역 정리 | ✅ **달성** |
| Report 17 §8 잔여 4건 (globalSent, cout, FileHandler, UIComponents) | ✅ **해소** |
| 슬라이드 6대 레거시 | ✅ **6/6 Resolved** |
| ctest·Golden Master 회귀 | ✅ **GREEN** |
| HTTP·CSV·HTML 계약 | ✅ **불변** (BOM·헤더·포트 8080) |
| `httplib.h` diff | ✅ **0** |
| Git 커밋 | ⏳ **미커밋** (워킹 트리 변경) |

**롤백:** 불필요. ctest 0 failures, Golden Master diff 없음.

### 9.1 권장 커밋 메시지 (참고)

```text
refactor(phase3): extract config constants and remove legacy smells

Add ServerConfig, CsvConstants, FilterConstants.
Remove TextAnalyzer globalSent/Kw, Filters cout, FileHandler, UIComponents.
Unify state access via FeedbackSession; add CSV field escaping.
```

---

## 10. 변경 이력

| 일자 | Phase | 변경 내용 |
|------|-------|-----------|
| 2026-05-22 | 진단 | 잔존 코드 스멜 목록화 (P1~P3, 슬라이드 대조) |
| 2026-05-22 | Phase 3 구현 | 상수 추출·static/cout/dead code 제거·FeedbackSession 통합·CSV escape |
| 2026-05-22 | 검증 | ctest 20/20 Pass, GM 4/4, 슬라이드 6/6 Resolved |
| 2026-05-22 | 보고서 | 본 문서 작성 — `Report/Refactoring/18.*` |

---

*본 문서는 Report 17 이후 **Phase 3 네이밍·상수·레거시 정리** 세션의 공식 기록이다.*
